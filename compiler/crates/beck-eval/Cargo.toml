[package]
name = "beck-eval"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Phase 1's Core backend: a tree-walking evaluator behind the Backend seam"

[dependencies]
beck-core.workspace = true
# The primitives that are a table or somebody else's parser — case mapping, the calendar, the
# digests — so the tree-walker and a compiled program call one implementation (docs/93 §93.12).
beck-prim.workspace = true
# JSON's text form only. The shape in between is Beck's own `Json` union, so this is a reader and
# a writer rather than a data model the language would then have two of.
serde_json.workspace = true
beck-diag.workspace = true

[lints]
workspace = true
