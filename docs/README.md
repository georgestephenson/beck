# Beck — implementation plan

One language for the frontend, the backend, the database, the container, and the cluster — where
every tier is a pure function or a fold, and time enters at declared merge points.

## Provenance

The plan derives from George's original conversation, preserved in
[`00-original-idea.md`](00-original-idea.md) (the share link wasn't machine-readable; the transcript
was supplied directly and is now the source of truth). A first draft of this plan was written before
the transcript was available; checking it against the original changed three load-bearing things:

- **The data tier is event-sourced, not relational-first**: the database is a `durable` fold over
  an event stream; queries are pure functions of that state, kept incremental by the compiler.
  Relational storage is a *substrate* (read models in Postgres), not the semantic model.
- **The browser is a patch interpreter by default** (~KBs of compiler residue), with a per-component
  upgrade to client-side rendering — not a fat-WASM-client-first design.
- **Deploys ride the stream**: typed `migrate`/`upcast` functions demanded at deploy, drain/resume
  choreography, replay as a feature.

## The three questions from the brief, answered

| Question | Short answer | Where |
|---|---|---|
| Python-like without losing the Lisp power? | **Yes — solved problem.** Homoiconicity is a property of the core AST, not the notation (Elixir/Julia/Nim prove it). One canonical `Node` AST, two faithful surfaces; the missing Python piece is a block-passing call form so macros receive indented bodies. The S-expression surface stays as the spec and macro-debugging notation | [`02`](02-syntax.md) |
| Dependencies best-in-class *and* open source? | Rust host; Salsa; dual Cranelift/LLVM codegen; thin-client first, Wasmtime for server-side WASM; **Postgres as durable substrate + MIT-licensed differential dataflow for incremental views** (Materialize-class capability without the BUSL licence); DataFusion for analytics; apko for reproducible daemonless images; kube-rs; Crossplane (Terraform is BUSL — excluded). Licences, benchmarks, swap costs per dependency | [`07`](07-dependencies.md) |
| Kubernetes under the hood? | Yes — as a **compiler backend** behind a `Platform` trait, never as language semantics. Infra is derived from program analysis: a `durable` fold ⇒ volume + snapshots; `merge_clients()` ⇒ websocket ingress; effect rows ⇒ least-privilege NetworkPolicy/RBAC/grants. `beck run` needs no cluster, container, or registry | [`06`](06-kubernetes-and-packaging.md) |

## Documents

| # | Document | What it covers |
|---|---|---|
| 00 | [The original idea](00-original-idea.md) | The seed conversation — sketch verbatim, requirements extracted |
| 01 | [Vision and premise](01-vision-and-premise.md) | The three moves from SICP to a language; the canonical example in the Python surface; prior art and lessons |
| 02 | [Syntax: Python surface, Lisp core](02-syntax.md) | Dual-surface design, macros, block-passing, hygiene, honest losses |
| 03 | [Type and effect system](03-type-and-effect-system.md) | Streams/signals/folds, the merge point, determinism, placement-as-effect, security proofs, migrations, modularity |
| 04 | [Compiler architecture](04-compiler-architecture.md) | Pipeline, IRs, signal-graph slicing, wire format, incrementality, `beck explain`, testing |
| 05 | [Tier lowering](05-tier-lowering.md) | Patch-interpreter client + Mode B, dual-backend server, log-as-database, infra object graph |
| 06 | [Kubernetes and packaging](06-kubernetes-and-packaging.md) | Images, derived manifests, the operator, deploys-ride-the-stream, dev→prod ladder |
| 07 | [Dependencies](07-dependencies.md) | Every third-party choice: rationale, licence, benchmarks, rejected alternatives, swap cost |
| 08 | [Roadmap](08-roadmap.md) | Phase 0 premise-proof, walking skeleton, phased plan, exit criteria, team shape |
| 09 | [Risks and open questions](09-risks-and-open-questions.md) | Ranked risks, decisions taken, remaining opens |
| 10 | [Decision log](10-decisions.md) | George's decisions D1–D20, all settled — including the Git/local-first analysis, the language-not-framework rationale, and the name: **Beck** |
| 11 | [Language tour](11-language-tour.md) | What Beck looks like, construct by construct — types, traits, macros, queries, UI, services, in-language tests |
| 12 | [Standards and conformance](12-standards-and-conformance.md) | The rulesets Beck conforms to, layer by layer — and the test-linked spec discipline that makes conformance real |
| 13 | [Testing strategy](13-testing.md) | Every applicable kind of test and why — built around the free oracles Beck's semantics manufacture (DST, differentials, TLA+, Jepsen) |
| 14 | [Review findings](14-review-findings.md) | The adversarial review: ranked security/design findings — all resolved (D14) |
| 15 | [Scale and distribution](15-scale-and-distribution.md) | The DDIA checklist against Beck's semantics; the scaling ladder (partitioned logs, typed sharding), sagas, and why Beck needs a fabric, not a Redis — with that checklist made executable as a four-verdict conformance matrix (§15.6) |
| 16 | [Packages and ecosystem](16-packages-and-ecosystem.md) | The Rails/npm lessons; Beck's own package system — **tarns** (packages), **forces** (vertical-slice feature packages), **the Mere** (index); effect-transparent dependencies, no install hooks |
| 17 | [The playground](17-playground.md) | The whole stack in a browser tab (the DST convergence), the cloud rung with the compiler as first sandbox, content-addressed sharing, playground-as-Beck-app |
| 18 | [Phase 0 report](18-phase-0-report.md) | **Built.** What [`phase0/`](../phase0/) implements, the measured exit criteria, the kill/pivot gates, and what turned out harder than expected |
| 19 | [Phase 1 report](19-phase-1-report.md) | **Built.** The compiler in [`compiler/`](../compiler/): what it compiles, the harnesses it passes, what it deliberately does not do, and the corrections Phase 1 makes to these documents |
| 20 | [Phase 2 report](20-phase-2-report.md) | **Built.** The moat: effect rows and their inference, placement solved from a cost model, `secret[T]`/`Sendable` with §3.5 as tests, `.becki` and separate compilation, `--wire-compat` — and the corrections Phase 2 makes to these documents |
| 21 | [Tests in Beck, and proof](21-tests-in-beck-and-proof.md) | **§21.2–§21.3 built** (see 22). `test` blocks that cross the boundary without a network, mocks derived from the effect row instead of written out — and the six-rung ladder that says how we know a generated manifest is right, with what a proof would and would not add |
| 22 | [Phase 3 report, part 1](22-phase-3-report.md) | **Built.** Tests written in Beck: `test`/`property` blocks, `beck test`, effect-atom stubs nobody writes, one type-directed generator — one bullet of Phase 3's twelve, with the other eleven named rather than implied |
| 23 | [Phase 3 report, part 2](23-general-slicer-report.md) | **Built.** The general slicer: the signal graph as a graph, several durable folds fused into one accumulator, shared computations bound once, every tier crossing enumerated — the debt two phases carried, and the silent mis-slice it was hiding. Plus `beck explain incremental`, the analysis §3.8 asks for and the plan is the input to — with the view engine still unbuilt, and the report saying so first |
| 24 | [Phase 3 report, part 3](24-incremental-views-report.md) | **Built.** The incremental view engine: a view compiled to a dataflow of operators and maintained from the change, `remaining` by ±1 per event at any size, recompute promoted to the CI oracle it was always supposed to be — with the page's children still assembled in full, a 3–5× constant factor rather than a new asymptote, and four times the per-subscriber memory, all measured |
| 25 | [Benchmarks and expressiveness](25-benchmarks-and-expressiveness.md) | The third-party performance suites, layer by layer, and which would measure a placeholder today — plus SICP as the expressiveness benchmark the premise has never had, with chapter 1 running and the six walls beyond it measured. Nand2Tetris, LeetCode, DDIA and the curriculum behind them assessed: which yield a test, which a lesson |
| 26 | [Phase 3 report, part 4](26-arrangement-sharing-report.md) | **Built.** One shared dataflow: the operators that do not read the session held once for every subscriber rather than once each, advanced per event rather than per connection, with a bounded change history for subscribers that render late and a rebuild for ones that render very late. 55× less work per event on a public feed and 1.3× on the sketch — because where a program reads the session is what decides its fanout cost. Plus §5.3's per-session memory, exported at last, and a stale-flag defect in `24`'s engine that only a fanout could show |
| 27 | [Phase 3 report, part 5](27-walls-report.md) | **Built.** Three of the six walls [`25`](25-benchmarks-and-expressiveness.md) measured between Beck and the rest of SICP: a library runs its own tests at last — so chapter 1's five-declaration wrapper is gone and every domain module can be unit-tested; a type may mention itself or anything declared later, with a comment thread added to the corpus to prove every pass survives one; and the `if` that typed one branch as the other's expectation, which cost exercise 1.43. Plus `sicp/ch2.beck`, which needed two of the three and could not have been written for either alone |
| 28 | [Releases and deployment](28-releases-and-deployment.md) | The three pipelines — releasing the compiler, deploying what `beck build` emits, and the project's own dogfood deployments — with what exists today (CI and nothing downstream of it), the signed-and-reproducible release plan, and the schedule of gates deliberately not added yet |
| 29 | [Domain-driven design](29-domain-driven-design.md) | The tactical-DDD mapping, pattern by pattern — native with stronger guarantees, or *dissolved* because the problem cannot arise; the vocabulary decision (claim the equivalence, refuse the jargon); BDD with prose rendered from tests and `.feature` parsing refused; and Evans's cargo-shipping system adopted as the DDD expressiveness benchmark, its walls forecast |
| 30 | [Bounded contexts and microservices](30-bounded-contexts-and-microservices.md) | **Designed, not built.** Strategic DDD: one log per context (revising D2, explicitly), cross-context reads a compile error, sagas as the only cross-context write path; contexts as the unit of deployment with the context map lowered to derived NetworkPolicy; the outside world on a three-rung honesty ladder down to foreign systems; heterogeneous hosting as constraints on derivation, never patches on output |
| 31 | [Phase 3 report, part 6](31-tail-calls-report.md) | **Built.** Proper tail calls, and the diagnostic that [`25`](25-benchmarks-and-expressiveness.md) §25.7 offered as an alternative to them — both, because neither is sufficient alone. 1,500 tail calls and 60,000 spend the same host stack, to the byte; a recursion that is *not* in tail position is bounded by a count rather than by whatever stack the process happened to have, and exceeding it is a message with a span instead of a `SIGABRT`. Plus `Backend::stack_bytes`, the first thing on the backend seam that is about a resource rather than about execution — and 13% of the evaluator's throughput, measured and not recovered |
| 32 | [Phase 3 report, part 7](32-numeric-tower-and-polymorphism-report.md) | **Built.** The last two of [`25`](25-benchmarks-and-expressiveness.md)'s six walls: reals, resolved from their operands rather than from a type class Beck does not have — so SICP §1.1.7 runs and prints `3.00009155413138`, which is the number on the book's page, digit for digit; and user-written polymorphism, with the type parameter rigid inside its own body and fresh at every call, measured across a `.becki` boundary rather than believed. Plus an ordering defect the reals brought with them that no SICP number could have caught, and a *new* wall the last one made visible: a `list[T]` cannot be taken apart |
| 33 | [Phase 3 report, part 8](33-effect-polymorphism-and-list-patterns-report.md) | **Built.** The two findings [`32`](32-numeric-tower-and-polymorphism-report.md) ended on. §3.2's `map : (list[a], (a -> b ! e)) -> list[b] ! e` is now true of a definition a *user* wrote — a pure caller of a generic higher-order function stays pure, however another caller uses it — and a `list[T]` can be taken apart, so SICP's `accumulate` and the whole of §2.2.3 are written the way the book writes them. Plus exhaustiveness for a `match` on a list, and `Pattern::binders`: the `_ => {}` in three IR passes that would have miscounted the day a list pattern reached them |
| 34 | [Generated documentation](34-generated-documentation-report.md) | **Built.** Doc comments in the language (`##`, metadata rather than a form, so documenting a definition is not an API change), `beck doc` for a module, and a language reference derived from the compiler's own tables — the effect/tier matrix is `Tier::discharges` *evaluated*, not transcribed. With the drift gate that fails when the checked-in reference and the compiler disagree, and what it does not do: no cross-module links, no versions, and `missing_docs` deliberately off across 865 public items |
| 35 | [The standards landscape, mid-2026](35-standards-landscape.md) | **Survey, not charter.** The external standards landscape as of mid-2026, assessed against [`12`](12-standards-and-conformance.md) §12.1's rule: the proposed starting points (ISO/IEC 24772, TR 10182, RM-ODP, Common Criteria, the 17000 series, 9646, 29119) each given a verdict; the gaps the charter lacks (POSIX.1-2024, RFC 9457, Wasm 3.0 — whose guaranteed tail calls are how a future WASM tier honours what [`31`](31-tail-calls-report.md) made language-level — UTS #39, the EU CRA); the IEEE 754 row checked against [`32`](32-numeric-tower-and-polymorphism-report.md)'s reals — what the digit-for-digit oracle backs, and what stays design; and the self-standardisation answer — publish our own, test-linked, per Phase 5 — the derived-and-gated discipline [`34`](34-generated-documentation-report.md) has now begun — with the two triggers that would ever justify a standards body |
| 36 | [Phase 3 report, part 9](36-parameterised-types-report.md) | **Built.** A type that takes a parameter: `model`, `union`, `newtype` and `type` each carry the type-parameter list a `def` has had since [`32`](32-numeric-tower-and-polymorphism-report.md), so SICP §2.2's tree is the book's tree — one `count_leaves` over `Tree[Int]`, `Tree[Str]` and `Tree[Tree[Int]]`, and a `tree_map` whose procedure may change the element type. With the two kinds of type parameter distinguished — a definition's is rigid, a declaration's is positional and survives into the stored declaration — arity declared rather than counted off the fields that happen to mention it, and the `.becki`/`--wire-compat` decision the refusal file asked to have taken deliberately: a declaration is published once, parameterised, and `Tree[Int]` and `Tree[Str]` are not the same boundary. Plus an error code retired because its premise stopped being true, and [`22`](22-phase-3-report.md) §22.6's test-checking pass finally lifted out of `check.rs` after eight reports of promising it |
| 37 | [Phase 3 report, part 10](37-traits-report.md) | **Built.** Traits, checked — the oldest unpaid debt in the project, and the warning that stood in for them since Phase 1 is retired. A `trait` declares signatures over an abstract `Self`; an `impl` writes the bodies and no types at all, because a second copy of the signature is a second place for it to be wrong. Coherence, the orphan rule, completeness, and the trait's effect row as the bound every impl is held to — which found a hole in what an *empty* declaration meant. Dispatch is static and an impl is **desugared into ordinary definitions**, so not one line of the evaluator, the splitter, the plan or the runtime changed. What is deliberately not here is the parametric half: no bounds on a type parameter, so generic code cannot call a trait method, `+` is still resolved ad-hoc, and §2.1.1's rationals still refuse |
| 38 | [The research literature, August 2026](38-literature-survey.md) | **Survey, not charter.** The academic literature checked against the unbuilt list and the open questions, area by area — bounds and dictionary passing (a near-complete instruction sheet: one resolver, coherence inherited from the orphan rule, dictionaries as the IR's ground truth), the view engine's three unbuilt pieces (fusion has a substrate, the arrangement lifecycle has a published answer, the delta-streamed page has none), the choreographic turn that supplies the slicer's missing correctness theory, errors and structured concurrency as row labels and handlers rather than mechanisms, the merge point confirmed as the industry attractor, and the two places the project is ahead of the record — with [`35`](35-standards-landscape.md)'s adopt/borrow/watch/decline verdicts and the triggers that would fire them |
| 39 | [Phase 3 report, part 11](39-bounds-report.md) | **Built.** Bounds — `def largest[T: Ord](xs: list[T])`, so generic code can call a trait method at last, which [`37`](37-traits-report.md) named as the single item everything else was waiting behind. The implementation is a **parameter**, not a data structure: a bounded definition gains one argument per method of each bound, named `Trait::method@T`, so finding an implementation is one lookup that returns a global for a concrete type and a local for a bounded parameter — and a bounded definition calling another passes its own straight through. No IR node and no evaluator case, for the second feature running. With the expectation consulted before the choice is made, three distinguishable failures, and the operator/numeric decision `rational.beck` needs written out as options rather than taken under time pressure |
| 40 | [Phase 3 report, part 12](40-traits-across-modules-report.md) | **Built.** A trait that crosses a module: a `.becki` carries the trait, the `impl` headers and the bounds, so a library can publish the half of itself [`39`](39-bounds-report.md) §39.6 said it could not. An impl header is a bodyless `impl`, refused in a module exactly as a bodyless `def` is; a trait crosses as *types* and is turned back into syntax on arrival, so an impl of an imported trait needs no special case. The orphan rule finally has two modules to be about. Plus `--wire-compat` for traits — where "a method added is breaking" is the same shape as "an event variant added is breaking" — and a diagnostic that had been pointing confidently at the wrong file since Phase 2 |
| 41 | [Phase 3 report, part 13](41-generic-arithmetic-report.md) | **Built.** Generic arithmetic, and the last refusal gone. `+`, `-`, `*` and `/` resolve through a prelude trait `Num` whose four method names are SICP §2.5.1's own, so `one_third() + one_third()` is `2/3` — printed as `2/3`, which is the number on the page and not the `0.6666666666666666` a real gives. The decision [`39`](39-bounds-report.md) §39.7 declined to take under time pressure, taken, with the book as the argument for taking it that way. Dispatch happens only where there is something to dispatch to, so `1 + true` is still a mismatch and `1 + 1.0` is still refused. `sicp/refusals/` is **empty** and its README says what that does and does not claim. Plus an orphan-rule hole that two reports could not reach, and could not be reached until a trait was in scope without being local |
| 42 | [Security assurance, August 2026](42-security-assurance.md) | **Survey and assessment, not charter.** The security, thread-safety and memory-safety posture read off the tree rather than off the design — what is *structural* (`forbid(unsafe)` inherited by all nine crates, one writer, ordered collections, bound SQL), what is *tested* (§3.5's suite, which tests each property by writing the program it forbids), and what is *absent* (a recursion bound in the front end, identity, quotas, a threat model, a disclosure policy) — with the one live defect measured: `beck check` aborts on 3,000 nested parens in a debug build, on the same 64 MiB stack [`adr/0007`](adr/0007-evaluator-stack-is-declared-not-discovered.md) declared for 4,000 evaluator frames. Then the August 2026 record against it: the memory-safety roadmap Beck already satisfies but has not written; verify-rust-std's null result, and what it says about where to aim verification instead; non-interference as the name of the `secret[T]` claim; SLSA v1.2's source track, CISA's 2026 SBOM elements, ASVS 5.0 and OWASP Top 10:2025 as four rows of [`12`](12-standards-and-conformance.md) that have moved; and a seven-rung ladder whose first four rungs are days of work |
| 43 | [The threat model](43-threat-model.md) | **Charter.** Who Beck defends against, and who it does not: four adversaries with what each is assumed able to do, the claims defended and by what *kind* of evidence (structural, tested, absent), seven things explicitly not defended, and the controls that do not exist — each of which is a failing test in `pending_security.rs` rather than a sentence, so the list cannot quietly go stale. With the trust boundaries drawn once, and the events that require this document to be edited |
| 44 | [Phase 3 report, part 14](44-wave-0-report.md) | **Built.** [`08`](08-roadmap.md) §8.5.4's Wave 0 — the set that was overdue, a one-way door, or a gate. The front end counts its own recursion at last, against a ceiling measured rather than declared, and the 64 MiB that was *already false* in one profile; the clock is supplied rather than ambient, three phases after F11 said it could not be retrofitted; the threat model and the disclosure policy exist; the absent controls fail out loud; identifiers are pinned to a Unicode version and a UTS #39 profile, with the half that profile does not reach — Trojan Source — closed separately. And the two syntax decisions [`09`](09-risks-and-open-questions.md) §9.6 held for four phases, taken — the first of which *splits*, because the implementation had already answered it as two questions |
| 45 | [Phase 3 report, part 15](45-error-rows-report.md) | **Built.** Failure as a row label: `raise` performs `raises(E)`, `try:` is the handler and `Result[T, E]` is what it produces. No mechanism was added, which is the whole argument — a fallible caller is fallible by inference, a `uses` clause bounds it, `.becki` publishes it, and `--wire-compat` calls a function that starts being able to fail a breaking change, in a sentence written for `net.out`. With row aliases taken before rows got long, a handler that catches only what it can name, and the placement bug that had been wrong for `partial` since Phase 2 |
| 46 | [Phase 3 report, part 16](46-standard-library-report.md) | **Built, first half.** The standard library, and the division it had to make about itself: a host's table or grammar is a primitive, composition is written in Beck — so thirty-one primitives and `compiler/lib/`, three libraries in the language with their own tests, gated on the directory rather than on a list. `json_parse` and `time_parse` **raise**, which is the whole of §8.5.3's trap 2 cashed. Time is Hinnant's calendar arithmetic rather than a dependency, because a replay must not disagree with the run it is replaying about what a date is. And the wall writing a library found that writing a compiler had not: a trait's row is a bound, so `Money` cannot have `+` |
| 47 | [Phase 3 report, part 17](47-effect-polymorphic-traits-report.md) | **Built.** The wall [`46`](46-standard-library-report.md) wrote, down a day later: a trait's declared row was a ceiling every impl was held to, so an operator was unavailable to every type whose operation can fail. It is now a floor — an impl's row is inferred, published with the impl so it crosses a module, and inherited by whoever calls it. Bounded generics turned out to have been effect-polymorphic all along and only the ceiling was in the way. Plus two corrections to `try:` that made it *more* precise: it resolves the row before reading it, and it catches the failure its signature names while the others travel |
| 48 | [Phase 3 report, part 18](48-identity-report.md) | **Built, first half.** Identity as a seam: an `Actor` with a private name that only a provider can mint, `DevIdentity` (the old behaviour, now saying so) and a `SignedIdentity` that verifies a keyed-BLAKE3 credential — so "only the owner may toggle their todo" stops being enforced against a string the caller chose. Both edges refuse before rendering; a refusal is coarse to the client and specific to the operator, and counted apart from a `validate` rejection. **Not OIDC**: no JWKS, no asymmetric signature, and claims do not reach the program — each asserted as absent rather than left to be inferred |
| 49 | [Phase 3 report, part 19](49-http-client-report.md) | **Built.** The outbound call, and the effect row [`46`](46-standard-library-report.md) said nobody had designed: `http_fetch(host, request)` performs `net.out(host)` for the host **written at the call site**, so the cluster's egress rule is that same string and a computed host is a compile error rather than a call the deployment cannot be told about. The second primitive whose row is a function of an argument, `raise` being the first. Plus the wall writing it found — §3.5 gives a program no way to read a `secret[Str]`, so a credential could not be put in a header, invisible for three phases because no program had ever tried to *spend* a secret — closed by sending it at the edge rather than by weakening the property. And the network arrives on F11's seam early, three implementations before there is a second caller. **Not TLS**, asserted as absent |
| 50 | [Phase 3 report, part 20](50-collections-and-dates-report.md) | **Built.** The rest of Wave 2 that is a library rather than a language feature: a `Set[T]` as a map's keys with the order a function of the values, sorting by a key rather than by a comparator, grouping and deduplication — and the civil calendar written in Beck, because RFC 3339 is a grammar and "which day is 2024-02-29 plus one" is arithmetic. Hinnant's two functions cross-checked against the same two in Rust over 5,000 days, with what that does and does not establish said plainly. No new primitives. Plus two findings, neither a wall: the diagnostic for a missing type argument told readers to write `Set[_]`, and there is no wildcard type — so a `check_arity` that knew the arity and not the names had been offering a program that does not compile since [`36`](36-parameterised-types-report.md), and the gate is now a test that applies the suggestion; and a record orders by its field *names* rather than as declared, so a two-key sort key written `Key(score=…, name=…)` sorts by the name — pinned rather than fixed, because a value carries no declaration and two orderings for one type would be worse than one surprising one |
| 51 | [Phase 3 report, part 21](51-arrangement-lifecycle-report.md) | **Built.** Two of the three loose ends [`26`](26-arrangement-sharing-report.md) §26.9 left on the shared dataflow, which turned out to be one missing rule: a **reader set**. A subscriber engine is counted while it lives and publishes how far it has rendered, so the change history is compacted to the oldest reader's frontier rather than to a constant 64, and the arrangements are released outright when the last subscriber goes — what a process holds is a function of who is connected rather than of what has ever connected. The frontier is an atomic and not a map entry, because it is written on every render and a lock there would serialise the concurrent renders sharing exists to allow; a reader that has *not* rendered pins nothing, because an engine with no arrangements rebuilds whatever it is offered. Retention is a policy — a ceiling over the frontier floor, and a switch for staying warm, since releasing costs the next subscriber a cold start. 99.1% of the shared side given back on `24-feed.beck`, one retained version instead of 64 on a fanout that keeps up, and a gauge that would have gone stale exactly when an operator needed it. The lock is **not** touched, and §51.7 says why that is now a harder change than it was |
| 52 | [Phase 3 report, part 22](52-crypto-and-identifiers-report.md) | **Built.** The crypto and identifier half of Wave 2: a digest, a keyed digest, a constant-time comparison, hex, base64url and a UUID reader that *normalises* — nine primitives and [`lib/crypto.beck`](../compiler/lib/crypto.beck), with no new dependency ([`adr/0015`](adr/0015-blake3-for-the-standard-librarys-digests.md)) because the hash the compiler already uses for node ids is a hash. A digest is **pure**, which is the line between it and the two nondeterministic things a crypto library is usually asked for. And this is where §3.5 met the first operation that *has* to turn a `secret[Str]` into a `Str` — a message authentication code exists to be handed to whoever must not learn the key, so [`49`](49-http-client-report.md)'s trick of moving *when* a secret is unwrapped does not reach it. One function declassifies, it performs `cap.sign` so no view can call it, and a test enumerates the prelude and asserts the count is one ([`adr/0014`](adr/0014-a-keyed-digest-is-the-one-declassifier.md)). Plus two findings that are not about cryptography: a `test` block cannot exercise a capability — so the layer of a library holding a key is the layer Beck cannot test, and the answer is to make it two lines — and nine match arms in the evaluator cost a thousand levels of recursion, which is [`adr/0007`](adr/0007-evaluator-stack-is-declared-not-discovered.md)'s budget being spent from the other end |
| 53 | [Phase 3 report, part 23](53-are-we-fast-yet-report.md) | **Built.** The benchmark harness [`50`](50-collections-and-dates-report.md) §50.6 and [`52`](52-crypto-and-identifiers-report.md) §52.6 both called the largest thing owed: nine of Are We Fast Yet's fourteen benchmarks in [`compiler/awfy/`](../compiler/awfy/), each verified against the constant the original's own `verifyResult` checks — read out of its source, not remembered — with the provenance and the MIT notice gated so they cannot be dropped. Wall-clock is printed and nothing is compared to anything, which is [`25`](25-benchmarks-and-expressiveness.md) §25.9's rule kept and §25.3's "adopt now, publish now, make them unflattering" kept at the same time. The findings are the point, and only one is about speed: `and` and `or` did **not** short-circuit, so a guard written as a conjunction did not guard and a benchmark written the way its original is written ran out of fuel — a defect nothing this project had written could see, now **fixed** in the checker rather than the evaluator (short-circuiting is a property of the language, not of one backend) with no IR node, no evaluator case and no runtime change, and `beck explain incremental` byte-for-byte identical over all 31 corpus and example programs either side of it; Beck has **no bitwise operators**, so `mandelbrot`'s bit-packing is arithmetic tested against what the operators mean; and a nested `if` is not a statement. Plus the one language change: a float literal may carry an exponent, because a table of constants transcribed in a notation the reader cannot check against the source by eye is a transcription with a place to hide |
| 55 | [Phase 3 report, part 24](55-bignums-report.md) | **Built.** Arbitrary-precision integers, in Beck: a sign and base-10,000 limbs, schoolbook multiplication and long division, `impl Num` so `+` and `/` are the operators — the last floor of the numeric tower [`25`](25-benchmarks-and-expressiveness.md) §25.7 ordered, under [`32`](32-numeric-tower-and-polymorphism-report.md)'s reals and [`41`](41-generic-arithmetic-report.md)'s rationals. No primitive and no dependency, which is [`lib/README.md`](../compiler/lib/README.md)'s division applied to the first case where refusing a good crate cost something. Only `div` can fail, so `a + b` stays pure and the checker refuses a `try:` over it. Known-right in three layers: the book's constants, `property` blocks checking `Big` against `Int`, and 1,200 assertions against Rust's `i128` on operands past what an `Int` holds — with §50.3's limit on what a cross-check buys restated rather than forgotten. Plus the coercion decision (every conversion named, none implicit, because an `Int` that silently widened would hide the cost of arbitrary precision) and the finding: `str_slice`'s third argument is a **count**, not an end index, the compiler's comment beside it said "byte offsets" when the behaviour is characters, and every other call site in the tree is one where both readings agree |
| 56 | [Phase 3 report, part 25](56-decimal-report.md) | **Built.** Arbitrary-precision decimal over [`55`](55-bignums-report.md)'s bignums — the last item of [`08`](08-roadmap.md) §8.5.4's **Wave 2**, which is now finished. `units × 10^-scale`, canonical so `1.50` and `1.5` are one value and `==` is numeric equality ([`54`](54-ordering.md)'s argument: the value order is a `Map`'s iteration, the digest and the patch stream). The decision §55.6 left, taken: `/` is **exact or it refuses**, because a division that silently picked a scale and a rounding rule would be guessing — with `divide_to` as the one that always answers, three rounding rules rather than one because picking one is picking a jurisdiction, and a bound that is honest about being partly chosen for the gate's sake. Checked against exact rational arithmetic in `i128` over 900 assertions whose **halfway cases are generated on purpose**, since exactly-half is the only input where the three rules disagree and a uniform sample never lands there. And this is the first `lib/` file to import a sibling, which is where all three findings came from: a rounded division that was not canonical, and `beck doc` wrong three ways about a module that imports another — it read one file, then published the merged program's 93 names instead of the module's 40, then reported 0 documented because `link` had never merged doc comments |
| 57 | [Phase 3 report, part 26](57-richards-report.md) | **Built.** Richards — the first of Are We Fast Yet's five **macro**-benchmarks, and the one thing [`53`](53-are-we-fast-yet-report.md) §53.6 said nothing was known about. Not a loop but a program: six tasks passing packets over a mutable object graph, which is the shape Beck has least of. Both of the suite's constants — 23,246 packets queued and 9,297 holds — matched on the first run, which two independent counters over 33,000 activations do not do by accident. The port needed one rule the nine micro-benchmarks did not: **a closure over mutable state is a function of the state**, so Java's `ProcessFunction` returning the next task and changing the scheduler on the way becomes one that takes the scheduler and returns both. A threading discipline, not a wall — and one place the port is *cleaner*, because a `union` of the four tasks' private data replaces a function field, four lambdas, four subclasses and a cast. The first benchmark where [`31`](31-tail-calls-report.md)'s tail calls are load-bearing rather than tidy. Plus §57.5: the other four assessed with line counts and what each needs, **none blocked by the language**, and the methodological question attached to the collection classes three of them share — using ours would be faster and wrong |
| 58 | [Phase 3 report, part 27](58-json-report.md) | **Built, and honest about what is not.** Json — the second macro-benchmark: a JSON parser over 25,820 characters, verified against the suite's own count of 156 operations. Beck *has* `json_parse`, and calling it would have made this a measurement of `serde` and a claim about nothing — the first program in the tree to decline a primitive for a methodological reason rather than a technical one. The one liberty is the one [`57`](57-richards-report.md) §57.5 forecast from the Java source alone: `str_slice` counts characters, so the input becomes a `list[Str]` once and the rest is a transcription. §58.4 names what the port does *less* of than the original — no hash index on objects — because "the port is faster" and "the port does less" are the same sentence and only one is honest. And §58.5 **sizes the three that are not here** — CD, DeltaBlue, Havlak — with line counts, published configurations and what each needs, plus the recommendation to attempt Havlak first because it is the only one with a cheap thing to learn: whether 52 traversals of a 5,213-node graph fit the fuel budget at all |
| 59 | [Phase 3 report, part 28](59-havlak-report.md) | **Built.** Havlak — loop recognition over a 5,213-node control-flow graph, verified against the suite's own 1,605 loops. [`58`](58-json-report.md) §58.5 said to attempt this one first because it was the only one with something *cheap to learn*, and it was right: the published configuration's fifty discarded runs **exhaust the evaluator's 50,000,000-step fuel budget** after 13.7 s in release, six fit where eight do not, and that is now the second benchmark the budget refuses — a pattern rather than an incident, and a `--fuel` owed. §59.5 is the finding, and it is a class of bug specific to benchmarks: the first version of the discarded-run loop passed the run's *loop count* back as its counter, so it ran once instead of fifty times — and **every verified number was still right**, because a discarded run produces no answer for `verifyResult` to check. The defence is to carry the discarded result somewhere a test can see it. Plus the detail transcribed rather than improved — `findSet` repoints at the starting node's parent, not at the root it found — and §59.2, which splits [`58`](58-json-report.md)'s open question: a `som.Vector` is a `list`, and a `som.Set` is not a `Map` |
| 60 | [Phase 3 report, part 29](60-collision-detection-report.md) | **Built.** CD — aircraft collision detection over a **red-black tree with parent pointers**, rebalanced on insert and on delete, which is the most mutation-shaped structure in the suite. 42 collisions, the suite's own number, over an arena of integer handles. The more interesting half is §60.2: it needed `Math.cos`, `Math.sin` and a `(int)` cast on a real, and Beck had **none of the three** — [`32`](32-numeric-tower-and-polymorphism-report.md) built the reals with `sqrt` and three phases never asked for more. `sin`, `cos` and `trunc` are primitives now, which makes this the **third** time Are We Fast Yet has found something the language was missing. It also corrects [`58`](58-json-report.md) §58.5's "none blocked by the language": that was answered from the Java's shape, and the gap was in its arithmetic. Plus §60.4 — the only file in `awfy/` with a test that is not the suite's own, because a red-black tree can be wrong in a way that still produces 42 |
| 61 | [Phase 3 report, part 30](61-deltablue-report.md) | **Built, and the suite is complete.** DeltaBlue — an incremental constraint solver over a **cyclic** object graph, which is the data structure rather than a way of writing one; the answer is two arenas that refer to each other by name, because that is what a cycle is once you cannot follow a pointer. **Are We Fast Yet is fourteen of fourteen.** It is the only benchmark in the suite that verifies against *nothing* — `innerBenchmarkLoop` returns `true` and the oracle is six `throw`s inside the planner — so all six are kept as a `raise`, and the file also asserts they **fire**, because a `raise` that could never happen verifies as much as none. Its published measurement size, 12,000, is the **third** benchmark to exhaust the evaluator's fuel budget, which stops being a finding and becomes a missing feature: `--fuel` is owed by three of the fourteen. §61.7 says what the whole harness establishes — expressiveness, not speed — and what it found on the way, none of it about performance |
| 62 | [Phase 3 report, part 31](62-fuel-report.md) | **Built.** `beck test --fuel`, which [`61`](61-deltablue-report.md) §61.3 named as owed by three of Are We Fast Yet's fourteen. The evaluator's 50,000,000-step budget was right as a runaway backstop and wrong as the only one available: **a backstop nothing can raise is a ceiling**, and three benchmarks legitimately live above it. All three now run at the size their suite measures at — `deltablue` at 12,000 in 1 m 51 s, `havlak` at its published 50 discarded runs in 1 m 32 s, and `mandelbrot` at size 500 producing **191**, which is the suite's *default* published constant and closes half of [`53`](53-are-we-fast-yet-report.md) §53.6's caveat. The gates still run the reduced configurations, because a suite taking three minutes a benchmark is one nobody runs — but that is now a decision about `cargo test` rather than a limit of the tool. Tested in both directions on one program, because a flag that raises a ceiling has to be shown raising it *and* shown the ceiling is still there without it |
| 63 | [Phase 3 report, part 32](63-felleisen-report.md) | **Built.** Felleisen's table — [`25`](25-benchmarks-and-expressiveness.md) §25.9's formal deliverable, and the *last* SICP item Phase 3 owed. For each of the seven special forms SICP introduces, one verdict: **recovered** (a macro, which is a *local* rewrite and is Felleisen's 1991 criterion) or **global**. **Six recovered, one conceded**, which is exactly the shape §25.9 forecast — and the conceded row is `amb`, which the forecast named. The concession is written out rather than asserted: CPS `amb` works, and needing a success and a failure continuation on *every function on the path* is what "global" means. `quote` came out better than forecast — the block was the recursive types and [`27`](27-walls-report.md) removed them — so what is lost there is notation, which §63.6 declines to call expressive power. `delay`, the row §25.9 said would put the whole claim in trouble, was blocked by an **off-by-one**: `() -> T` parsed and failed to check, because nothing in the prelude, the corpus, either chapter, the standard library or fourteen benchmarks had ever written a function type taking no arguments |
| 64 | [Phase 3 report, part 33](64-compile-speed-report.md) | **Built.** The compile-speed budget [`25`](25-benchmarks-and-expressiveness.md) §25.9 scheduled and [`13`](13-testing.md) §13.7 listed — and **it found a quadratic in the front end within an hour of existing**, which is the argument for the whole exercise. It gates a *shape* rather than a rate, because §13.7's "a gate that flakes gets deleted" rules out a wall-clock threshold on a shared runner: cost per declaration must not grow with the number of declarations, along three axes because a module grows three ways. Placement computed each definition's "what if it moved" explanation by **re-summing the whole program**, three times per definition — `O(n × (n + e))`, computed whether anybody asked for an explanation or not. 12,800 definitions took 6.2 s and now take 0.58 s, with `beck explain place` byte-for-byte identical over all 57 programs and all 2,817 lines of per-candidate costs. Two things it records rather than fixes: a residual `n^1.35` in `check` along a long call chain, and that `MAX_NESTING` does not count a **flat** block — sequential local bindings still abort the process — at 12,000 in a debug build and 100,000 in a release one, so **which programs compile depends on how the compiler was built**, which is [`42`](42-security-assurance.md) §42.2's defect on an axis its ceiling does not measure and [`adr/0007`](adr/0007-evaluator-stack-is-declared-not-discovered.md)'s property the evaluator has and the front end does not. §13.7's keystroke→diagnostic latency has a number at last: 4.7 ms worst, 0.75 ms median, over every Beck program in the tree |
| 65 | [Phase 3 report, part 34](65-lsp-report.md) | **Built.** `beck lsp` — diagnostics as you type, hover, go-to-definition and document symbols, from the same front end `beck check` runs. One of the Phase 3 bullets [`26`](26-arrangement-sharing-report.md) §26.9 lists as untouched, and it is here now because [`64`](64-compile-speed-report.md) made the case for the design: re-checking the whole buffer is the right choice when the largest real file costs 4.7 ms. §4.6's "no separate language server implementation to drift" is the claim it has to keep, so the harness compares the server's answers to **the library's** rather than to strings written in the harness — a squiggle against `compile_or_library_str`'s own diagnostic, a hover against `iface::render_item`, symbols against `Interface::of`. Measured end to end through the real protocol: **0.84 ms** at 59 lines, **7.37 ms** at 914, and §4.6's 100 ms budget holds to **about 13,000 lines in one module and not beyond** — which is where the Salsa query graph §4.6 describes stops being optional. Takes no LSP framework ([`adr/0016`](adr/0016-the-language-server-speaks-json-rpc-directly.md)) |
| 66 | [Phase 3 report, part 35](66-page-snapshots-report.md) | **Built.** `expect page matches snapshot` and `beck test --update` — the last open question in [`21`](21-tests-in-beck-and-proof.md) §21.2, named and left by [`22`](22-phase-3-report.md) §22.6. `contains` asserts one string somebody thought to name; a snapshot asserts **every attribute, every `data-b-*` binding and the order of the list**, which is what the client interprets. Three decisions separate an assertion from a habit, and §21.2 named the risk that makes each one matter: a **missing** snapshot fails rather than writing silently, writing is **only ever** `--update`, and the diff is in the failure. That last took a second attempt — the first elided both sides from the start of the line, which on a one-line page shows two identical prefixes and hides the difference, so the window is centred on the first differing *character*. `insta` was pointed at by §21.2 and not used: it snapshots a Rust value from a Rust test, and this is a Beck page from a Beck test. `examples/todo.beck` now asserts its whole page |
| 67 | [Phase 3 report, part 36](67-sqlite-report.md) | **Built.** `SqliteLog` — a third durable substrate, one of [`26`](26-arrangement-sharing-report.md) §26.9's untouched bullets. The reason is [`07`](07-dependencies.md) §7.8.1's and it is not speed: **SQLite is also a read-model engine**, so "append and project in one transaction" — what Postgres gives production — becomes available in a file, which redb cannot offer at any speed because it has no query language. That property is asserted both ways: one transaction moves the log *and* a projection, and a rolled-back one moves neither. And a finding worth the report on its own — the first version reported SQLite at **26× redb**, and that number was a `synchronous = NORMAL` against redb's fsync: a weaker *promise*, not a faster engine. At equal durability the two are within noise. §3.7 makes the log the only description of a program's history, so durability is now a public `Durability` enum defaulting to fsync, visible in `kind()`, with `beck bench log` printing both rows so the flattering number is never the only one on screen |
| 68 | [Phase 3 report, part 37](68-clbg-report.md) | **Built.** [`compiler/clbg/`](../compiler/clbg/README.md) — seven of the Computer Language Benchmarks Game's ten, each verified against the Game's **own published output file** — which is the **last item on [`25`](25-benchmarks-and-expressiveness.md) §25.9's Phase 3 row**. [`64`](64-compile-speed-report.md) §64.7.1 had held it back for a reason rather than a doubt: the Game's expected outputs were unreachable, and "a suite that measures Beck against numbers this repository made up" is worse than no suite ([`59`](59-havlak-report.md) §59.5 is the precedent). So the files are checked in and the oracle is **enforced**: `clbg.rs` rebuilds each asserted literal from the published file, and recomputes the digest for the two 10 KB ones. A wrong constant fails the Beck test; a wrong constant with a matching wrong expectation fails the Rust one. Three findings, and the largest is not about speed — **`lib/` is a standard library that nothing outside `lib/` can import**, because `import` resolves only against the root module's own directory. Nothing had noticed because nothing had tried: `lib/` files import each other and no other directory in three phases had reached across one. That is what stops `pidigits`, and it is left as a decision for [`10`](10-decisions.md) rather than repaired here. `mandelbrot` (a binary PBM, and `Str` is UTF-8) and `regexredux` (no regex) are the other two of the ten |
| 69 | [Phase 3 report, part 38](69-standard-library-imports-report.md) | **Built.** `import bignum` from any directory — [`68`](68-clbg-report.md) §68.4's finding, which that report left as a decision rather than a repair: the standard library was reachable from `lib/` and from nowhere. [`10`](10-decisions.md) D23 is the rule — an import resolves against the caller's directory **first** and the library second, so adding to the library can never break a program that never asked for it — and [`adr/0018`](adr/0018-the-standard-library-is-carried-in-the-compiler.md) is why the library is carried *in the compiler* rather than found on a path: the prelude's primitives always were, and the Beck half is written against them version for version. The interesting half is what it broke. Beck's namespace is flat, so **every module in `lib/` now has to link with every other**, and two collisions had been waiting where nothing could reach them — `is_negative` in two files, `pow10` in two more. The gate that found them is one program importing the whole directory. Pointing outward the same property is a cost rather than a defect, found by writing a probe called `one()`: importing `bignum` reserves `one`, `zero`, `divide` and `trim` in the importing program, and qualified imports are [`16`](16-packages-and-ecosystem.md) §16.7's to build. Also here: `clbg/format.beck` becomes `lib/format.beck` (§68.4's third finding), an import costs about 12 µs a line with no cache (§69.4), and **`pidigits` is ported** — the benchmark §68.6 called "the only one that is owed a fix" — verified against the Game's published `N = 30` output and measuring `lib/bignum.beck` rather than GMP. That port cost the `clbg` gate two minutes and needed a `--fuel` flag to run at all, which turned out to be the wrong thing to accept: §69.6 is [`55`](55-bignums-report.md) §55.6's "the thing to replace first if any of it is ever a bottleneck", arrived at — Knuth's *estimate* now **brackets** long division's trial-digit search instead of replacing it, so the digit is still the one the search finds and the search still proves it. 100 million evaluator steps become under 16 million, the flag is gone, the gate is 52 s, and `lib/decimal.beck`'s own tests — which nobody touched — got 2.3× faster. Asking the same question one level further down found something larger and **not fixed** (§69.7): `list_append` copies the whole list, so the tail-recursive accumulator every loop in the language is written as — `lib/`, `awfy/`, `clbg/`, the corpus, both SICP chapters — is **O(n²) in time**, which is [`19`](19-phase-1-report.md) §19.4's fold defect in a second place. Worse, the evaluator's own budget cannot see it: over the same loop the wall clock quadruples per doubling and the step count exactly doubles, because a primitive that copies ten thousand values is one step. The cheap fix (`Arc::try_unwrap` when the append holds the only reference) was tried and measured at no change — the caller's frame still binds the list — so the real fix is last-use moves or a persistent sequence, and both want their own change |
| 70 | [Phase 3 report, part 39](70-last-use-moves-report.md) | **Built.** Last-use moves — [`69`](69-standard-library-imports-report.md) §69.7's defect fixed by the option it recommended. The compiler computes which read of a local is its **last** (`beck_core::liveness`), the frame hands that value over instead of lending it, and `list_append` pushes into a list nobody else holds: the accumulator idiom every Beck loop is written as goes from **O(n²) to O(n)**, 25× faster at 8,000 elements and unbounded beyond. A wrong analysis cannot corrupt anything — a frame is emptied only when `Arc::get_mut` proves nothing else holds it — so both bugs announced themselves as missing bindings: a definition's body is *itself* a lambda, so the closure rule excluded every function in the program and the pass marked nothing (found by measuring, not by testing); and **a call's arguments are evaluated before its callee**, so walking backwards has to meet the callee first — `sicp/ch1.beck`'s exercise 1.32 failed against the book's own answer within seconds of the first full run. §70.4 is the part that changed the design: moving *every* last read made the suite **6–13% slower**, because moving an `Int` costs more than cloning one, so the move is now conditional on the value being worth moving and the result is neutral (−8% to +3%) rather than a speed-up. §70.5 is a defect found on the way and fixed: the evaluator's counted ceiling of 4,000 was **unreachable in a debug build**, which aborted the process at 1,000 levels instead of diagnosing — the stack was sized from the cheapest function body imaginable. And §70.6 is the audit: **strings are quadratic twice over** (`+` copies both sides, `str_len` and `str_slice` walk characters) and are not fixed, against four things measured and found sound — the weight-balanced `Map`, `O(1)` list indexing, linear frame chains, and the linear sorts and digests |
| 71 | [Phase 3 report, part 40](71-strings-report.md) | **Built.** A string that knows its own length — [`70`](70-last-use-moves-report.md) §70.6's other two quadratics, fixed. `Value::Str` was an `Arc<str>` with no spare capacity, so `+` allocated and copied both sides; `str_len` counted characters and `str_slice` skipped to its start. Now a string carries its character count and an ASCII flag, computed once on construction out of the scan the copy was already doing, and holds a `String` so `+` can **push** into it when [`70`](70-last-use-moves-report.md)'s analysis proves nobody else holds it — the same mechanism as `list_append`, which is what makes the analysis worth having twice. Building text: ×3.47 per doubling → **×1.93**, 232 ms → 86 ms at 64,000 characters. Walking it by index: ×2.71 → **×1.99**, and 2.2× cheaper per character even at 1,000 because `str_len` stopped walking the string in the loop guard. Neutral on today's programs (−2.7% to +2.9%), and `fasta` is the row that explains why: it writes 10,245 characters and does not move, because it builds them with `str_join` — the cliff was under the *obvious* way to write it. §71.3 is the gate and the mistake in it: the first version spanned 8× and **passed against the old evaluator**, because a memcpy is invisible next to an evaluator step until the string is long enough; over 16× the old code costs ×4.07 and the fixed one ×0.71 |
| 72 | [Phase 3 report, part 41](72-space-and-constants-report.md) | **Built.** The constants, after three reports on asymptotics — and the constant had a factor of three in it. A Rust enum is as wide as its widest variant, so **every `Value` was 48 bytes** because of the record variant that most values are not: an `Int`, a `Bool`, every element of a million-element list, every binding in every frame. The record went behind a pointer and a `Value` is **16 bytes**, a frame binding 56 → 24, a 100,000-element list of integers 4.8 MB → 1.6 MB. Measured: `havlak` **−23.4% peak memory** and −5.6% time, `deltablue` −11%, `json` −5.7% memory. Also here: **`--fuel` charges for work** — a primitive that touches `n` elements costs `n`, so the budget bounds what a program can make the evaluator do rather than how many nodes it visits, which is [`70`](70-last-use-moves-report.md) §70.7's defect. The first version of that charge was wrong in the way it warns about — `list_slice` charged the length of the list it sliced *from*, a 500× overcharge that `knucleotide` caught by needing 178M fuel — and with it corrected every program in the tree still fits the **unchanged** 50M default. Non-ASCII text now carries a chunked character index, closing [`71`](71-strings-report.md) §71.5's first row, built eagerly because a lazy one is interior mutability inside a `Map` key. And because the budget can see a copy at last, the accumulator's shape has a gate with **no clock in it** |
| 73 | [Phase 3 report, part 42](73-closures-share-their-code-report.md) | **Built, and it is one line.** A closure held its body **by value**, and `Core` is a tree of `Box`es — so evaluating a `lam` deep-copied the whole function body, once per call of a named function. Calling a 50-node function copied 50 nodes before running any of them. Measured by holding the work still and growing the callee: 20,000 calls whose executed path never changes cost 42 ms, 227 ms and 606 ms as the *unexecuted* part of the body grows, and **18.8, 20.8, 21.8 ms** once the body is shared. **Every benchmark in the tree is 56–72% faster** — decimal −72%, pidigits −70%, json −69%, havlak −66% and 6 MB smaller, knucleotide −65%, deltablue −56% — with no change to any answer. §73.3 is why it survived four reports about performance: it is not asymptotic, so measuring one program at two sizes shows a straight line, and it is invisible to `--fuel` even after [`72`](72-space-and-constants-report.md) made the budget count work, because the copy is not work a *primitive* does. What found it was asking what calling a function *should* cost — a frame and a jump — and then making the callee bigger without making it do more. §73.8 is the uncomfortable half: a third of the tree-walker's reputation was this, and "the interpreter is a placeholder" is exactly the sentence the standard forbids using to explain a bad number |
| 54 | [Ordering](54-ordering.md) | **Options, not a decision.** What [`50`](50-collections-and-dates-report.md) §50.5 left for the next person: `Ord` as a trait, written out with its costs. The value order turns out not to be a library convenience — it is the `Map`'s iteration, the arrangements' collation, the state digest and the patch stream a replay must reproduce bit for bit — so an option that lets one type have two orders has to answer for the wire. Four options: leave it; `Ord` the way `Num` is a trait; that plus a comparator carried through every container; or give a record's *value* its declaration order. The recommendation is the last or the first, and explicitly **not** the trait — because the one thing it buys, a type overriding its own order, is the one thing that costs, and `<` is already polymorphic so a bound buys nothing. [`32`](32-numeric-tower-and-polymorphism-report.md) §32.2's reals are the precedent: change the representation so the single order is the right one |
| ref | [The language reference](reference/README.md) | **Generated.** The error index (102 codes), the command reference, the effect and tier matrix, the prelude and the forms — regenerated by `beck doc reference` and gated on every pull request. The design lives above; this is what a program is checked against |
| adr | [Decision records](adr/README.md) | Engineering decisions, one immutable record each — the why lives here so docs and comments can state current truth. Design decisions keep their D-numbers in [`10`](10-decisions.md) |

## The one-paragraph version

`beck` is a statically typed, homoiconic language with a Python-like surface in which the original
`(my-javascript (my-css (my-html)))` becomes literal: the page is a pure function of state, state is
a durable fold over an event stream, and hand-written JavaScript disappears — it's compiler residue.
Clients propose typed commands; one declared merge point admits time and nondeterminism; everything
downstream is deterministic, so replay, time-travel debugging and optimistic UI come from the
semantics rather than from frameworks. Placement is typed and checkable (secrets provably cannot
reach the browser), views are kept incremental by the compiler, and `beck build` emits not a binary
but a deployable system — reproducible OCI images plus a Kubernetes object graph derived from the
program's own effects — which `beck deploy` applies with typed migrations gating the rollout.
