# Beck — implementation plan

One language for the frontend, the backend, the database, the container, and the cluster — where
every tier is a pure function or a fold, and time enters at declared merge points.

## Provenance

The plan derives from George's original conversation, preserved in
[`00-original-idea.md`](00-original-idea.md) (the share link wasn't machine-readable; the transcript
was supplied directly and is now the source of truth). A first draft of this plan was written before
the transcript was available; checking it against the original changed three load-bearing things:

- **The data tier is event-sourced, not relational-first**: the database is a `durable` fold over
  an event stream; queries are pure functions of that state, kept incremental by the compiler.
  Relational storage is a *substrate* (read models in Postgres), not the semantic model.
- **The browser is a patch interpreter by default** (~KBs of compiler residue), with a per-component
  upgrade to client-side rendering — not a fat-WASM-client-first design.
- **Deploys ride the stream**: typed `migrate`/`upcast` functions demanded at deploy, drain/resume
  choreography, replay as a feature.

## The three questions from the brief, answered

| Question | Short answer | Where |
|---|---|---|
| Python-like without losing the Lisp power? | **Yes — solved problem.** Homoiconicity is a property of the core AST, not the notation (Elixir/Julia/Nim prove it). One canonical `Node` AST, two faithful surfaces; the missing Python piece is a block-passing call form so macros receive indented bodies. The S-expression surface stays as the spec and macro-debugging notation | [`02`](02-syntax.md) |
| Dependencies best-in-class *and* open source? | Rust host; Salsa; dual Cranelift/LLVM codegen; thin-client first, Wasmtime for server-side WASM; **Postgres as durable substrate + MIT-licensed differential dataflow for incremental views** (Materialize-class capability without the BUSL licence); DataFusion for analytics; apko for reproducible daemonless images; kube-rs; Crossplane (Terraform is BUSL — excluded). Licences, benchmarks, swap costs per dependency | [`07`](07-dependencies.md) |
| Kubernetes under the hood? | Yes — as a **compiler backend** behind a `Platform` trait, never as language semantics. Infra is derived from program analysis: a `durable` fold ⇒ volume + snapshots; `merge_clients()` ⇒ websocket ingress; effect rows ⇒ least-privilege NetworkPolicy/RBAC/grants. `beck run` needs no cluster, container, or registry | [`06`](06-kubernetes-and-packaging.md) |

## Documents

| # | Document | What it covers |
|---|---|---|
| 00 | [The original idea](00-original-idea.md) | The seed conversation — sketch verbatim, requirements extracted |
| 01 | [Vision and premise](01-vision-and-premise.md) | The three moves from SICP to a language; the canonical example in the Python surface; prior art and lessons |
| 02 | [Syntax: Python surface, Lisp core](02-syntax.md) | Dual-surface design, macros, block-passing, hygiene, honest losses |
| 03 | [Type and effect system](03-type-and-effect-system.md) | Streams/signals/folds, the merge point, determinism, placement-as-effect, security proofs, migrations, modularity |
| 04 | [Compiler architecture](04-compiler-architecture.md) | Pipeline, IRs, signal-graph slicing, wire format, incrementality, `beck explain`, testing |
| 05 | [Tier lowering](05-tier-lowering.md) | Patch-interpreter client + Mode B, dual-backend server, log-as-database, infra object graph |
| 06 | [Kubernetes and packaging](06-kubernetes-and-packaging.md) | Images, derived manifests, the operator, deploys-ride-the-stream, dev→prod ladder |
| 07 | [Dependencies](07-dependencies.md) | Every third-party choice: rationale, licence, benchmarks, rejected alternatives, swap cost |
| 08 | [Roadmap](08-roadmap.md) | Phase 0 premise-proof, walking skeleton, phased plan, exit criteria, team shape |
| 09 | [Risks and open questions](09-risks-and-open-questions.md) | Ranked risks, decisions taken, remaining opens |
| 10 | [Decision log](10-decisions.md) | George's decisions D1–D20, all settled — including the Git/local-first analysis, the language-not-framework rationale, and the name: **Beck** |
| 11 | [Language tour](11-language-tour.md) | What Beck looks like, construct by construct — types, traits, macros, queries, UI, services, in-language tests |
| 12 | [Standards and conformance](12-standards-and-conformance.md) | The rulesets Beck conforms to, layer by layer — and the test-linked spec discipline that makes conformance real |
| 13 | [Testing strategy](13-testing.md) | Every applicable kind of test and why — built around the free oracles Beck's semantics manufacture (DST, differentials, TLA+, Jepsen) |
| 14 | [Review findings](14-review-findings.md) | The adversarial review: ranked security/design findings — all resolved (D14) |
| 15 | [Scale and distribution](15-scale-and-distribution.md) | The DDIA checklist against Beck's semantics; the scaling ladder (partitioned logs, typed sharding), sagas, and why Beck needs a fabric, not a Redis — with that checklist made executable as a four-verdict conformance matrix (§15.6) |
| 16 | [Packages and ecosystem](16-packages-and-ecosystem.md) | The Rails/npm lessons; Beck's own package system — **tarns** (packages), **forces** (vertical-slice feature packages), **the Mere** (index); effect-transparent dependencies, no install hooks |
| 17 | [The playground](17-playground.md) | The whole stack in a browser tab (the DST convergence), the cloud rung with the compiler as first sandbox, content-addressed sharing, playground-as-Beck-app |
| 18 | [Phase 0 report](18-phase-0-report.md) | **Built.** What [`phase0/`](../phase0/) implements, the measured exit criteria, the kill/pivot gates, and what turned out harder than expected |
| 19 | [Phase 1 report](19-phase-1-report.md) | **Built.** The compiler in [`compiler/`](../compiler/): what it compiles, the harnesses it passes, what it deliberately does not do, and the corrections Phase 1 makes to these documents |
| 20 | [Phase 2 report](20-phase-2-report.md) | **Built.** The moat: effect rows and their inference, placement solved from a cost model, `secret[T]`/`Sendable` with §3.5 as tests, `.becki` and separate compilation, `--wire-compat` — and the corrections Phase 2 makes to these documents |
| 21 | [Tests in Beck, and proof](21-tests-in-beck-and-proof.md) | **§21.2–§21.3 built** (see 22). `test` blocks that cross the boundary without a network, mocks derived from the effect row instead of written out — and the six-rung ladder that says how we know a generated manifest is right, with what a proof would and would not add |
| 22 | [Phase 3 report, part 1](22-phase-3-report.md) | **Built.** Tests written in Beck: `test`/`property` blocks, `beck test`, effect-atom stubs nobody writes, one type-directed generator — one bullet of Phase 3's twelve, with the other eleven named rather than implied |
| 23 | [Incremental views](23-incremental-views-report.md) | **Built — [`03`](03-type-and-effect-system.md) §3.8 and [`05`](05-tier-lowering.md) §5.3, complete.** The slicer, the plan, the engine, one shared dataflow, its lifecycle, the read models and query fusion, in one chapter. `remaining` updates by ±1 per event at ten rows and at five thousand — but assembling the page is still linear, so the end-to-end win is a 3–5× constant factor and §23.19 says so. The finding a developer should take from it is §23.10: **where you read the session decides what your fanout costs**, 55× on a public feed against 1.3× on the sketch. Its transferable lessons are §23.16 — a refusal never exercised on the shape it exists to refuse is a claim rather than a check, and an aggregate is where a performance defect hides from a correctness oracle. |
| 25 | [Benchmarks and expressiveness](25-benchmarks-and-expressiveness.md) | The third-party performance suites, layer by layer, and which would measure a placeholder today — plus SICP as the expressiveness benchmark the premise has never had, with chapter 1 running and the six walls beyond it measured. Nand2Tetris, LeetCode, DDIA and the curriculum behind them assessed: which yield a test, which a lesson |
| 27, 31–33, 36–41, 45, 47 | [The walls come down](27-the-walls-come-down-report.md) | **Built.** The type and effect system's features, in the order the walls forced. [`25`](25-benchmarks-and-expressiveness.md) §25.6 measured six walls between Beck and the rest of SICP and §25.7 put them in dependency order; the order was right, and each report's "what is still not" was the next one's brief. **The six**: a library runs its own tests, a type may mention itself, `B0320`'s `if`, proper tail calls *and* a depth diagnostic (the either/or was wrong — 1,500 and 60,000 tail calls spend the **same** host stack, byte for byte, at 13% of the evaluator's throughput), reals whose oracle is the book's own printed `3.00009155413138` to the digit, and user-written polymorphism. **Then what removing them made visible**: a generic definition's row was shared by every call site, so a pure caller was published as effectful because another caller was not — a row per call site now; and a `list[T]` that could not be taken apart. **Then the features those unblocked**: `union Tree[T]` and every other declaration taking the parameter list a `def` had, traits checked at last (`B0306` retired), bounds with the dictionary as a parameter, a trait that crosses a `.becki`, and `Num` — so SICP §2.5.1's rationals read `x + y` and `sicp/refusals/` **empties**. **Then Waves 1 and 2b**: an error is a row label with `Result` as its reified form, adopted from Koka rather than invented; and a trait's declared row became a floor rather than a ceiling, which is what let the standard library have an `impl Num for Money`. Five defects found on the way, none of them in the feature being built — including an orphan rule too weak, caught by a prelude trait. *Consolidated from eleven reports* |
| 28 | [Releases and deployment](28-releases-and-deployment.md) | The three pipelines — releasing the compiler, deploying what `beck build` emits, and the project's own dogfood deployments — with what exists today (CI and nothing downstream of it), the signed-and-reproducible release plan, and the schedule of gates deliberately not added yet |
| 29 | [Domain-driven design](29-domain-driven-design.md) | The tactical-DDD mapping, pattern by pattern — native with stronger guarantees, or *dissolved* because the problem cannot arise; the vocabulary decision (claim the equivalence, refuse the jargon); BDD with prose rendered from tests and `.feature` parsing refused; and Evans's cargo-shipping system adopted as the DDD expressiveness benchmark, its walls forecast |
| 30 | [Bounded contexts and microservices](30-bounded-contexts-and-microservices.md) | **Designed, not built.** Strategic DDD: one log per context (revising D2, explicitly), cross-context reads a compile error, sagas as the only cross-context write path; contexts as the unit of deployment with the context map lowered to derived NetworkPolicy; the outside world on a three-rung honesty ladder down to foreign systems; heterogeneous hosting as constraints on derivation, never patches on output |
| 34 | [Generated documentation](34-generated-documentation-report.md) | **Built.** Doc comments in the language (`##`, metadata rather than a form, so documenting a definition is not an API change), `beck doc` for a module, and a language reference derived from the compiler's own tables — the effect/tier matrix is `Tier::discharges` *evaluated*, not transcribed. With the drift gate that fails when the checked-in reference and the compiler disagree, and what it does not do: no cross-module links, no versions, and `missing_docs` deliberately off across 865 public items |
| 35 | [The standards landscape, mid-2026](35-standards-landscape.md) | **Survey, not charter.** The external standards landscape as of mid-2026, assessed against [`12`](12-standards-and-conformance.md) §12.1's rule: the proposed starting points (ISO/IEC 24772, TR 10182, RM-ODP, Common Criteria, the 17000 series, 9646, 29119) each given a verdict; the gaps the charter lacks (POSIX.1-2024, RFC 9457, Wasm 3.0 — whose guaranteed tail calls are how a future WASM tier honours what [`27`](27-the-walls-come-down-report.md) made language-level — UTS #39, the EU CRA); the IEEE 754 row checked against [`27`](27-the-walls-come-down-report.md)'s reals — what the digit-for-digit oracle backs, and what stays design; and the self-standardisation answer — publish our own, test-linked, per Phase 5 — the derived-and-gated discipline [`34`](34-generated-documentation-report.md) has now begun — with the two triggers that would ever justify a standards body |
| 38 | [The research literature, August 2026](38-literature-survey.md) | **Survey, not charter.** The academic literature checked against the unbuilt list and the open questions, area by area — bounds and dictionary passing (a near-complete instruction sheet: one resolver, coherence inherited from the orphan rule, dictionaries as the IR's ground truth), the view engine's three unbuilt pieces (fusion has a substrate, the arrangement lifecycle has a published answer, the delta-streamed page has none), the choreographic turn that supplies the slicer's missing correctness theory, errors and structured concurrency as row labels and handlers rather than mechanisms, the merge point confirmed as the industry attractor, and the two places the project is ahead of the record — with [`35`](35-standards-landscape.md)'s adopt/borrow/watch/decline verdicts and the triggers that would fire them |
| 42 | [Security assurance, August 2026](42-security-assurance.md) | **Survey and assessment, not charter.** The security, thread-safety and memory-safety posture read off the tree rather than off the design — what is *structural* (`forbid(unsafe)` inherited by all nine crates, one writer, ordered collections, bound SQL), what is *tested* (§3.5's suite, which tests each property by writing the program it forbids), and what is *absent* (a recursion bound in the front end, identity, quotas, a threat model, a disclosure policy) — with the one live defect measured: `beck check` aborts on 3,000 nested parens in a debug build, on the same 64 MiB stack [`adr/0007`](adr/0007-evaluator-stack-is-declared-not-discovered.md) declared for 4,000 evaluator frames. Then the August 2026 record against it: the memory-safety roadmap Beck already satisfies but has not written; verify-rust-std's null result, and what it says about where to aim verification instead; non-interference as the name of the `secret[T]` claim; SLSA v1.2's source track, CISA's 2026 SBOM elements, ASVS 5.0 and OWASP Top 10:2025 as four rows of [`12`](12-standards-and-conformance.md) that have moved; and a seven-rung ladder whose first four rungs are days of work |
| 43 | [The threat model](43-threat-model.md) | **Charter.** Who Beck defends against, and who it does not: four adversaries with what each is assumed able to do, the claims defended and by what *kind* of evidence (structural, tested, absent), seven things explicitly not defended, and the controls that do not exist — each of which is a failing test in `pending_security.rs` rather than a sentence, so the list cannot quietly go stale. With the trust boundaries drawn once, and the events that require this document to be edited |
| 44 | [Phase 3 report, part 14](44-wave-0-report.md) | **Built.** [`08`](08-roadmap.md) §8.5.4's Wave 0 — the set that was overdue, a one-way door, or a gate. The front end counts its own recursion at last, against a ceiling measured rather than declared, and the 64 MiB that was *already false* in one profile; the clock is supplied rather than ambient, three phases after F11 said it could not be retrofitted; the threat model and the disclosure policy exist; the absent controls fail out loud; identifiers are pinned to a Unicode version and a UTS #39 profile, with the half that profile does not reach — Trojan Source — closed separately. And the two syntax decisions [`09`](09-risks-and-open-questions.md) §9.6 held for four phases, taken — the first of which *splits*, because the implementation had already answered it as two questions |
| 46 | [The standard library](46-standard-library-report.md) | **Built — [`08`](08-roadmap.md) Phase 3's standard-library bullet, plus the Benchmarks Game harness beside it.** Strings, collections, JSON, time, sets, dates, durations, money, an HTTP client and `compiler/lib/` — importable by name since [`10`](10-decisions.md) D23. Its subject is **the division** (§46.1): a host's table or grammar is a primitive, composition is a file in Beck, and the line is not what is fast but what has a definition in the language. Testing that against real callers found four walls — a trait's row is a bound so `Money` had no `+`; a credential could not be sent; `lib/` was a library nothing outside it could import; a record orders by field **name** — and two costs: the accumulator idiom is quadratic where the step budget cannot see it (§46.12), and a benchmark demanding enough to make a cost visible made every caller of bignum division 2.3× faster (§46.13). |
| 48 | [Identity](48-identity-report.md) | **Built — [`10`](10-decisions.md) D6 and Phase 3's identity bullet, with no unbuilt row left.** A dev-mode provider that says what it is, a symmetric one, an OIDC relying party with PKCE, claims that reach `validate`, `external`/`managed()` as declarations, and presence. Three ideas carry it, and none is about OIDC: **an actor is something the runtime decides** rather than something a client asserts (§48.2), which was the oldest absent control in the project; **the issuer is a declaration** because §6.5 derives egress from what a program names, and a runtime with a peer the compiler never heard of emits a policy refusing its own JWKS fetch (§48.7); and **claims stop at the log** (§48.6). §48.12 is the grep that would have fired and still proves nothing. |
| 52 | [Phase 3 report, part 22](52-crypto-and-identifiers-report.md) | **Built.** The crypto and identifier half of Wave 2: a digest, a keyed digest, a constant-time comparison, hex, base64url and a UUID reader that *normalises* — nine primitives and [`lib/crypto.beck`](../compiler/lib/crypto.beck), with no new dependency ([`adr/0015`](adr/0015-blake3-for-the-standard-librarys-digests.md)) because the hash the compiler already uses for node ids is a hash. A digest is **pure**, which is the line between it and the two nondeterministic things a crypto library is usually asked for. And this is where §3.5 met the first operation that *has* to turn a `secret[Str]` into a `Str` — a message authentication code exists to be handed to whoever must not learn the key, so [`46`](46-standard-library-report.md)'s trick of moving *when* a secret is unwrapped does not reach it. One function declassifies, it performs `cap.sign` so no view can call it, and a test enumerates the prelude and asserts the count is one ([`adr/0014`](adr/0014-a-keyed-digest-is-the-one-declassifier.md)). Plus two findings that are not about cryptography: a `test` block cannot exercise a capability — so the layer of a library holding a key is the layer Beck cannot test, and the answer is to make it two lines — and nine match arms in the evaluator cost a thousand levels of recursion, which is [`adr/0007`](adr/0007-evaluator-stack-is-declared-not-discovered.md)'s budget being spent from the other end |
| 53, 57–61 | [Are We Fast Yet, in Beck](53-are-we-fast-yet-report.md) | **Built.** All fourteen benchmarks, each verified against the original suite's own constant, nine micro and five macro — Richards, Json, Havlak, CD, DeltaBlue. What it establishes is **expressiveness**: every feature of the common subset has a Beck shape, including the five nobody knew were expressible. It establishes nothing about speed, and [`25`](25-benchmarks-and-expressiveness.md) §25.9 still holds every comparative claim until there is a second backend. Three findings, none about performance: `and`/`or` did not short-circuit (fixed in the checker, so it is the language's property rather than one backend's), the language had no `sin`, `cos` or truncation, and it has no bitwise operators. **Three of the fourteen do not fit the 50,000,000-step fuel budget** at their published size, which turned a finding into the missing feature [`62`](62-fuel-report.md) went on to build. Plus the bug class only a benchmark has: a warm-up loop that never ran, invisible to the benchmark's own oracle because a discarded run produces no answer to check (§53.6). *Consolidated from six reports* |
| 55 | [Phase 3 report, part 24](55-bignums-report.md) | **Built.** Arbitrary-precision integers, in Beck: a sign and base-10,000 limbs, schoolbook multiplication and long division, `impl Num` so `+` and `/` are the operators — the last floor of the numeric tower [`25`](25-benchmarks-and-expressiveness.md) §25.7 ordered, under [`27`](27-the-walls-come-down-report.md)'s reals and [`27`](27-the-walls-come-down-report.md)'s rationals. No primitive and no dependency, which is [`lib/README.md`](../compiler/lib/README.md)'s division applied to the first case where refusing a good crate cost something. Only `div` can fail, so `a + b` stays pure and the checker refuses a `try:` over it. Known-right in three layers: the book's constants, `property` blocks checking `Big` against `Int`, and 1,200 assertions against Rust's `i128` on operands past what an `Int` holds — with §46.5's limit on what a cross-check buys restated rather than forgotten. Plus the coercion decision (every conversion named, none implicit, because an `Int` that silently widened would hide the cost of arbitrary precision) and the finding: `str_slice`'s third argument is a **count**, not an end index, the compiler's comment beside it said "byte offsets" when the behaviour is characters, and every other call site in the tree is one where both readings agree |
| 56 | [Phase 3 report, part 25](56-decimal-report.md) | **Built.** Arbitrary-precision decimal over [`55`](55-bignums-report.md)'s bignums — the last item of [`08`](08-roadmap.md) §8.5.4's **Wave 2**, which is now finished. `units × 10^-scale`, canonical so `1.50` and `1.5` are one value and `==` is numeric equality ([`54`](54-ordering.md)'s argument: the value order is a `Map`'s iteration, the digest and the patch stream). The decision §55.6 left, taken: `/` is **exact or it refuses**, because a division that silently picked a scale and a rounding rule would be guessing — with `divide_to` as the one that always answers, three rounding rules rather than one because picking one is picking a jurisdiction, and a bound that is honest about being partly chosen for the gate's sake. Checked against exact rational arithmetic in `i128` over 900 assertions whose **halfway cases are generated on purpose**, since exactly-half is the only input where the three rules disagree and a uniform sample never lands there. And this is the first `lib/` file to import a sibling, which is where all three findings came from: a rounded division that was not canonical, and `beck doc` wrong three ways about a module that imports another — it read one file, then published the merged program's 93 names instead of the module's 40, then reported 0 documented because `link` had never merged doc comments |
| 62 | [Phase 3 report, part 31](62-fuel-report.md) | **Built.** `beck test --fuel`, which [`53`](53-are-we-fast-yet-report.md) §53.3 named as owed by three of Are We Fast Yet's fourteen. The evaluator's 50,000,000-step budget was right as a runaway backstop and wrong as the only one available: **a backstop nothing can raise is a ceiling**, and three benchmarks legitimately live above it. All three now run at the size their suite measures at — `deltablue` at 12,000 in 1 m 51 s, `havlak` at its published 50 discarded runs in 1 m 32 s, and `mandelbrot` at size 500 producing **191**, which is the suite's *default* published constant and closes half of [`53`](53-are-we-fast-yet-report.md) §53.7's caveat. The gates still run the reduced configurations, because a suite taking three minutes a benchmark is one nobody runs — but that is now a decision about `cargo test` rather than a limit of the tool. Tested in both directions on one program, because a flag that raises a ceiling has to be shown raising it *and* shown the ceiling is still there without it |
| 63 | [Phase 3 report, part 32](63-felleisen-report.md) | **Built.** Felleisen's table — [`25`](25-benchmarks-and-expressiveness.md) §25.9's formal deliverable, and the *last* SICP item Phase 3 owed. For each of the seven special forms SICP introduces, one verdict: **recovered** (a macro, which is a *local* rewrite and is Felleisen's 1991 criterion) or **global**. **Six recovered, one conceded**, which is exactly the shape §25.9 forecast — and the conceded row is `amb`, which the forecast named. The concession is written out rather than asserted: CPS `amb` works, and needing a success and a failure continuation on *every function on the path* is what "global" means. `quote` came out better than forecast — the block was the recursive types and [`27`](27-the-walls-come-down-report.md) removed them — so what is lost there is notation, which §63.6 declines to call expressive power. `delay`, the row §25.9 said would put the whole claim in trouble, was blocked by an **off-by-one**: `() -> T` parsed and failed to check, because nothing in the prelude, the corpus, either chapter, the standard library or fourteen benchmarks had ever written a function type taking no arguments |
| 64 | [Phase 3 report, part 33](64-compile-speed-report.md) | **Built.** The compile-speed budget [`25`](25-benchmarks-and-expressiveness.md) §25.9 scheduled and [`13`](13-testing.md) §13.7 listed — and **it found a quadratic in the front end within an hour of existing**, which is the argument for the whole exercise. It gates a *shape* rather than a rate, because §13.7's "a gate that flakes gets deleted" rules out a wall-clock threshold on a shared runner: cost per declaration must not grow with the number of declarations, along three axes because a module grows three ways. Placement computed each definition's "what if it moved" explanation by **re-summing the whole program**, three times per definition — `O(n × (n + e))`, computed whether anybody asked for an explanation or not. 12,800 definitions took 6.2 s and now take 0.58 s, with `beck explain place` byte-for-byte identical over all 57 programs and all 2,817 lines of per-candidate costs. Two things it records rather than fixes: a residual `n^1.35` in `check` along a long call chain, and that `MAX_NESTING` does not count a **flat** block — sequential local bindings still abort the process — at 12,000 in a debug build and 100,000 in a release one, so **which programs compile depends on how the compiler was built**, which is [`42`](42-security-assurance.md) §42.2's defect on an axis its ceiling does not measure and [`adr/0007`](adr/0007-evaluator-stack-is-declared-not-discovered.md)'s property the evaluator has and the front end does not. §13.7's keystroke→diagnostic latency has a number at last: 4.7 ms worst, 0.75 ms median, over every Beck program in the tree |
| 65 | [Phase 3 report, part 34](65-lsp-report.md) | **Built.** `beck lsp` — diagnostics as you type, hover, go-to-definition and document symbols, from the same front end `beck check` runs. One of the Phase 3 bullets [`23`](23-incremental-views-report.md) §23.19 lists as untouched, and it is here now because [`64`](64-compile-speed-report.md) made the case for the design: re-checking the whole buffer is the right choice when the largest real file costs 4.7 ms. §4.6's "no separate language server implementation to drift" is the claim it has to keep, so the harness compares the server's answers to **the library's** rather than to strings written in the harness — a squiggle against `compile_or_library_str`'s own diagnostic, a hover against `iface::render_item`, symbols against `Interface::of`. Measured end to end through the real protocol: **0.84 ms** at 59 lines, **7.37 ms** at 914, and §4.6's 100 ms budget holds to **about 13,000 lines in one module and not beyond** — which is where the Salsa query graph §4.6 describes stops being optional. Takes no LSP framework ([`adr/0016`](adr/0016-the-language-server-speaks-json-rpc-directly.md)) |
| 66 | [Phase 3 report, part 35](66-page-snapshots-report.md) | **Built.** `expect page matches snapshot` and `beck test --update` — the last open question in [`21`](21-tests-in-beck-and-proof.md) §21.2, named and left by [`22`](22-phase-3-report.md) §22.6. `contains` asserts one string somebody thought to name; a snapshot asserts **every attribute, every `data-b-*` binding and the order of the list**, which is what the client interprets. Three decisions separate an assertion from a habit, and §21.2 named the risk that makes each one matter: a **missing** snapshot fails rather than writing silently, writing is **only ever** `--update`, and the diff is in the failure. That last took a second attempt — the first elided both sides from the start of the line, which on a one-line page shows two identical prefixes and hides the difference, so the window is centred on the first differing *character*. `insta` was pointed at by §21.2 and not used: it snapshots a Rust value from a Rust test, and this is a Beck page from a Beck test. `examples/todo.beck` now asserts its whole page |
| 67 | [Phase 3 report, part 36](67-sqlite-report.md) | **Built.** `SqliteLog` — a third durable substrate, one of [`23`](23-incremental-views-report.md) §23.19's untouched bullets. The reason is [`07`](07-dependencies.md) §7.8.1's and it is not speed: **SQLite is also a read-model engine**, so "append and project in one transaction" — what Postgres gives production — becomes available in a file, which redb cannot offer at any speed because it has no query language. That property is asserted both ways: one transaction moves the log *and* a projection, and a rolled-back one moves neither. And a finding worth the report on its own — the first version reported SQLite at **26× redb**, and that number was a `synchronous = NORMAL` against redb's fsync: a weaker *promise*, not a faster engine. At equal durability the two are within noise. §3.7 makes the log the only description of a program's history, so durability is now a public `Durability` enum defaulting to fsync, visible in `kind()`, with `beck bench log` printing both rows so the flattering number is never the only one on screen |
| 70–79 | [How the evaluator got fast](70-the-evaluator-gets-fast-report.md) | **Built.** Ten pieces of work on the tree-walker: **three quadratics removed** — the accumulator (last-use moves, so `list_append` pushes into a list nobody else holds), text (a `Str` that knows its own length), and the same loop written as a fold — and **six constants**, the largest of which was one `clone`: a closure was deep-copying its whole body on every construction, worth −56% to −72% on every benchmark. Also a call at 287→174 ns, mimalloc after `callgrind` found glibc's `malloc` at 35% of all instructions, a `let` at 134→32 ns, a 16-byte `Value`, and a record's field order decided once by a pass. **5.2× on `fib(30)`**, from 33× CPython to within a small multiple of the bytecode interpreters — and §25.9 still holds the comparative claim. Four changes were built, measured and **thrown away**; an instruction profile over-promised the wall clock by a factor of three; and a gate that could not fail, twice over, is the pattern `AGENTS.md` now points at. *Consolidated from ten reports* |
| 80 | [Structured concurrency](80-structured-concurrency-report.md) | **Built — `parallel:`, on a thread each, stopping the children after one that fails.** Three things carry the chapter. **The soundness is in the checker, not the scheduler** (§80.2): no child may name another and none may perform an effect another could observe, so running them together needed no analysis and no happens-before argument. **"After it" is the whole of cancellation, and it cost a defect to learn** (§80.7) — the obvious stop-everybody signal broke the ordering test eight times in forty runs of the whole suite and never when run alone. And **a language feature can be almost entirely a set of refusals** (§80.11): one primitive, one evaluator case, and a round-trip property stated in a doc comment that nothing had ever checked. |
| 81 | [Phase 3 report, part 50](81-fs-is-two-atoms-report.md) | **Built, the day after it was named.** `fs(path)` becomes `fs.read(path)` and `fs.write(path)` — [`80`](80-structured-concurrency-report.md) §80.12's first open item, and the one atom in §3.2's list that named a resource without saying what was being done to it. It was not wrong for three phases because nothing had asked it a question it could not answer; `parallel:`'s rule asked one, and the answers for a read and a write differ. So a scope may now have children that **read** files and still refuses children that write them — one line of the checker, which is what the split was for. Nothing else moved: `Tier::discharges` never looked at the operation, both atoms still break replay (a file can change between replays, which is a fact about *reading*), and both are stubbable. The precedent was already in the same list — §3.8's escape hatches have always been `external.read`/`external.write`, split for the same reason and without anybody writing down that it was a reason. §81.5 is the correction, and it is to [`80`](80-structured-concurrency-report.md)'s own justification: that report said §6.5 derives a volume's mount options from this atom, and **it does not** — `beck-infra` reads `ingress`, `durable` and `net.out` and has never looked at `fs`. The conclusion survives and the argument for it held for one reason rather than two; a capability was read out of a design document as though it were built, which is [`67`](67-sqlite-report.md) §67.3's failure with a capability where that one had a number. §81.6 is the honest limit: **no primitive touches a file**, so both atoms reach a row only through a `uses` clause — this makes the vocabulary correct rather than making filesystem access available |
| 82 | [Phase 3 report, part 51](82-the-defaults-that-should-be-unavoidable-report.md) | **Built, and one field of it could not have been written the day before.** [`06`](06-kubernetes-and-packaging.md) §6.5 has named four pod defaults as what "should be *unavoidable*, because they are what separates 'generated YAML' from 'production-grade generated YAML'" — non-root, a read-only root filesystem, dropped capabilities, `seccomp: RuntimeDefault` — and the emitter produced **one** of them. Now it produces all four, plus the `revisionHistoryLimit` the same sentence names. Three are constants applied to every container, including the substrate's: nothing a Beck program can do needs a Linux capability, needs to gain privileges partway through, or needs a syscall outside the runtime default. The fourth is **derived** — `readOnlyRootFilesystem` is true unless the program's own row carries `fs.write` — which is what [`81`](81-fs-is-two-atoms-report.md) §81.6 said the atom split made possible and did not build, and which one `fs(path)` atom could not express in either direction. The default is the secure one and the row is what relaxes it, so a program that writes and forgets to say so gets a refused write rather than a writable container. §82.3 names the asymmetry rather than leaving it to be found: the Postgres container gets the three constants and *not* the read-only root, because it is somebody else's image and a derived manifest may make claims about the program's own and not about a dependency's — asserted as an absence, so nobody adds it later from the app container's example. §82.4 is what is **not** established: `conformance.rs` skipped, there is no cluster here, and "the emitter writes these fields" is a different claim from "the pod starts" — with the `/tmp` risk named, and the check that can be made without a cluster made instead (the deployed path is `--store postgres` or `--store memory`, and `beck-rt`'s only file writes are on neither) |
| 83 | [Phase 3 report, part 52](83-the-runtime-edge-report.md) | **Built.** Two of [`42`](42-security-assurance.md) §42.6's four bullets about "what an untrusted client can do to a running Beck app today", closed. The websocket handshake passed `None` for its configuration, so the limits were tungstenite's — 64 MiB a message, 128 KiB of **eagerly-allocated** read buffer per connection, an unbounded write buffer — bounded well, but by somebody else's judgement; they are now 256 KiB a message and a frame, 8 KiB of read buffer and 8 MiB of write buffer, with §83.2's argument for each and a unit test asserting every one is *tighter* than the library's so a drift back is a decision. And nothing inspected `Origin`, so a page on any host could open a socket to a Beck app with whatever the visitor's browser carries; the upgrade now compares `Origin`'s authority against `Host` and answers `403`. §83.3 is the three ways that could have gone: an absent `Origin` is **allowed** (the attack needs a browser and every browser sends one), the scheme is **not** compared (§6.5's gateway terminates TLS in front of a plaintext hop), and there is **no allowlist** (a Beck app serves its own page, so same-origin is a description rather than a policy). §83.4 is the part that is about testing rather than security: this is the **first test in the project to drive `beck-rt`'s HTTP edge** — every session harness goes through an in-memory duplex, which is right for a subscription and means the handshake in front of it had never been exercised, and a refusal tested only as a pure function is one refactor from never being called. §83.5 corrects §42.6's own smaller item, which had **already been fixed and never recorded** — the escaper it calls incomplete escapes quotes and has for some time. §83.7 is what that pair establishes: a list of absences with tests attached survives; the same list as prose does not |
| 84 | [Phase 3 report, part 53](84-a-quota-is-only-as-good-as-its-actor-report.md) | **Built, and two things in it matter more than the quota.** [`14`](14-review-findings.md) F3 has been `APPROVED` and unbuilt since the review — "quotas are **on by default** with generous limits" — and is now 600 events a minute per actor, charged in `App::propose` *before* the ingress queue and from the **same instant that goes on the envelope**, so §3.7's "one place time enters" and F11's injected clock both survive. The counters are **sharded** into a fixed 1,024-bucket table, because the obvious map-from-actor is unbounded memory keyed by a string the client chooses — the same denial of service one level down — and the hash is keyed per process so a client cannot pick a name that shares somebody's budget. §84.4 is the first thing that matters more: a per-actor bound is worth **what the actor is worth**, so under the default `DevIdentity` — where the client names itself — rotating names spreads across the table rather than being stopped, and the real bound is `buckets × limit`. That sentence is the multiplication of two facts [`14`](14-review-findings.md) and [`48`](48-identity-report.md) each hold half of and neither states. §84.5 is the second: **both `pending_security.rs` gates guarding this gap stayed green through the change that closed it** — one grepped for identifiers the implementation did not happen to use, the other proposed 200 events against a limit that turned out to be 600. A proxy for a control is defeated by naming, and a behavioural test for an absence cannot be calibrated against a limit that does not exist yet. Fourth gate-that-could-not-fail, after [`70`](70-the-evaluator-gets-fast-report.md) §70.7's three and [`80`](80-structured-concurrency-report.md) §80.11's — and the pattern across all four is that the gate was written by the person who knew the gap, and tested the shape of the *gap* rather than the shape of the *fix* |
| 85 | [Phase 3 report, part 54](85-what-the-generator-found-report.md) | **Built, and it found three.** [`42`](42-security-assurance.md) §42.9 pinned grammar-aware fuzzing with the trigger "the bound lands", and the bound landed in [`44`](44-wave-0-report.md). §42.2 has quoted the Scriban advisory since — *a limit added at the one production somebody thought of is bypassed through a different one* — and the tree contained **three violations of it**. The type grammar was **not counted at all** (`list[list[…]]` 80,000 deep aborted while parens the same depth were refused). `primary` **released the counter before the recursion happened**, so `g(g(g(…)))` returned the depth to zero at every level — a counter on the right function measuring the wrong interval. And `1 + 1 + 1 + …` is read by a **loop**, so no recursion counter can see it: the Pratt parser builds a left-leaning tree of the same depth without recursing, and 300,000 terms died before anything could report. That third one is the lesson — **a counter counts recursion done, and the stack cares about tree depth built**, which are the same number only when the parser is recursive. Plus the flat-block axis [`64`](64-compile-speed-report.md) §64.4 measured and left unfixed, where a debug build aborted at 12,000 bindings and a release build at 100,000: `MAX_BLOCK` is 2,048 with a measured 6.8 KiB a statement behind it. §85.1 is the methodological half and is unflattering: the **first version of the generator passed and meant nothing**, because its sizes stopped at 3,000 — past every ceiling and short of every abort. A fuzzer calibrated against the limits you built tests that your limits work; one calibrated against the failures you had tests whether you found all the places they happen. §85.4 is what it cost elsewhere — two gates moved, and the one that *fired* is worth naming beside [`84`](84-a-quota-is-only-as-good-as-its-actor-report.md) §84.5's two that did not |
| 86 | [Getting started](86-getting-started.md) | **A guide, and it is compiled.** [`08`](08-roadmap.md) §8.5.4 names one remaining reason Phase 3's exit criterion cannot be attempted — "what it measures is documentation an outside developer could build from, and **there is none**" — and this is that, for the shape of program Beck is for: a function and a test, then state as a fold, then the whole application, then what the compiler worked out on its own and what `beck build` emits from it. Every ```` ```beck ```` block is a complete module that `beck-cli/tests/getting_started.rs` **compiles and runs the tests of**, and every `beck <command>` it shows is checked against the binary's own help — the discipline [`34`](34-generated-documentation-report.md) applied to the reference, because a guide whose examples do not compile teaches a newcomer that the documentation lies. §86.8 is what it does **not** establish, and the first line is the important one: the exit criterion is a claim about a *person*, it needs an outside developer, and nobody outside this project has read this. What changed is that the answer to "from what?" is no longer "there is nothing". Writing it also found a diagnostic that told a beginner to add a line they had already written — `NOT_AN_APPLICATION` is three codes and the message hardcoded one of them, so a module with a merge point and no client signal was told to add `merge_clients()` |
| 87 | [Phase 3 report, part 55](87-the-chapter-that-argues-back-report.md) | **Built.** SICP **chapter 3** — [`08`](08-roadmap.md) §8.5.4's "more of SICP, chapter 3 being the part closest to what Beck is for", and the chapter the language disagrees with. §3.1 introduces `set!`; §3.4 spends thirteen pages on two processes sharing mutable state; and §3.5 arrives forty pages later at a stream formulation of the same bank account and says *"this implementation is fully functional … yet it embodies changing state"* — which is [`01`](01-vision-and-premise.md) §1.1's premise in the book's own words, and why the file ends by asserting that §3.1.1's fold and §3.5.5's stream produce the same balances. Twenty-six `test` blocks and a `property` against the book's printed answers, including §3.3.4's half-adder at `sum 8`, `carry 11`, `sum 16` — times that are a consequence of the whole simulation — and three tables of doubles in §3.5.3 asserted digit for digit. It produced **no wall**: every section is expressible and the reorganisation is one rule applied twelve times. What it produced instead was three things that are not about expressiveness. **§87.4**: a program could declare `model Int` and the name then meant the record in one definition and the builtin in the next, silently — for fourteen of the sixteen builtin type constructors, the two exceptions being covered by accident. `B0314` has refused the same shadowing for a *type parameter* since [`27`](27-the-walls-come-down-report.md), consulting the same table; a declaration was the production it did not cover, which is [`85`](85-what-the-generator-found-report.md) §85.7 one report later from the other direction. `B0317` is the fix. **§87.5**: `x.with(f = g(x.f))` was **quadratic** where the record constructor spelling of it is linear — 520 steps an element at 1,000 and 4,028 at 8,000 — because `with` held the base's clone, and therefore a second reference to `x.f`, while `g` read it. That is [`19`](19-phase-1-report.md) §19.4's defect in a **fourth** spelling, and it survived [`70`](70-the-evaluator-gets-fast-report.md) and [`70`](70-the-evaluator-gets-fast-report.md) for [`70`](70-the-evaluator-gets-fast-report.md) §70.2's exact reason: nothing in the tree accumulates into a `list` or a `Str` field through `with`. 20.0 steps an element at both sizes now, **201× at 8,000**, −53% on text and within a control's noise on everything else. **§87.7**: the one thing chapter 3 cannot express, and it is a cost rather than an inexpressibility — §3.5.1's memoised `delay` is two `set!`s, so §3.5.3's tableau is **×5.2 per term** and nine terms is past the whole fuel budget. The book's nine numbers are here anyway, from a tableau whose rows are lists, which is a local rewrite and therefore no loss by Felleisen's criterion. §87.8 is the uncomfortable half: the quadratic was found by writing the gate that answers §3.3.2's **argument** for mutation, and "amortised Θ(1)" in a comment would have been the end of it |
| 90 | [Phase 3 report, part 58](90-nested-patterns-report.md) | **Built, and the last named remainder on a Phase 3 bullet is gone.** Nested patterns — `case Some(Circle(r))`, `case [Circle(r), *rest]`, `case Yes(Circle(0))` — the "pattern matching completion" half of [`08`](08-roadmap.md)'s concurrency-and-errors bullet, and the retirement of `B0345`, whose message said *"not available in Phase 1"* through three phases. The feature is small: `Pattern` becomes recursive, the checker recurses with the field's type, the evaluator matches a field the way it matches a scrutinee. **The exhaustiveness check is the work**, because it was a set of variant *names* and `case Some(Circle(r))` **names `Some` and covers a fraction of it** — a set-based check can only be wrong in one of two directions, letting through a program that crashes on `Some(Square(…))`, or refusing one that is exhaustive and demanding a `case _` that can never run. It is Maranget's usefulness algorithm now ([`exhaust.rs`](../compiler/crates/beck-core/src/check/exhaust.rs)), with two things that are Beck's rather than the paper's: **a list is `nil`/`cons` for the length of this check and nowhere else**, which is what lets one algorithm answer for a list and a union and which retired the two magic strings that stood in for a list's shapes; and the top level is asked one constructor at a time, so a `match` missing three variants still names three. One message got **better by accident** — `case [a, b]` was told "the empty list and a list with elements", which is true and useless, and is now told `[_]`, which is an input to write a test with. The same machinery answers a second question for free: **which arms can never match**, `B0355`, a warning rather than an error because `case _` after every variant is a habit and refusing a habit changes what compiles. §90.5 is the honest half: a pattern is an ordinary call form, so what refuses a 384-deep one is the *parser's* counter and not the checker's new one — which is kept anyway, because three times in this project's history a limit at one production has been reached through another, and §90.7 records that the guard cannot be made to fire so the next person need not rediscover it. Guards, or-patterns and `@` bindings are what remain, listed rather than implied |
| 91 | [Phase 3 report, part 59](91-guards-and-alternatives-report.md) | **Built, and no Phase 3 bullet has a named remainder left.** Everything [`90`](90-nested-patterns-report.md) §90.6 held back: **or-patterns** (`case Circle(r) | Square(r):`), **guards** (`case x if x < 0:`) and **`@` bindings** (`case whole @ Circle(r):`, whose binder is irrefutable so the pattern under it decides coverage — the forecast held and nothing else changed). Neither needed a new algorithm — [`90`](90-nested-patterns-report.md)'s exhaustiveness matrix already answers both, because an or-pattern is *several rows* and a guarded arm is *no row*: whether a guarded arm matches depends on a value rather than a shape, so counting it would call a `match` exhaustive on a condition that can be false, and the mirror rule is that a guarded arm above cannot make a later one dead. `|` is an ordinary infix operator at the lowest precedence — [`02`](02-syntax.md) §2.6's "patterns *are* expressions", with the checker refusing it elsewhere (`B0357`), and the token was free because [`53`](53-are-we-fast-yet-report.md) found Beck has no bitwise operators. A `case` pattern is read at binding power **1** rather than 0, which is the whole grammar change: the postfix `a if c else b` is offered only at 0, so the `if` after a pattern is unambiguously the guard's, and `|` still binds tighter. Every alternative must bind the same names at the same types (`B0356`), and their variables are unified onto the first's so the body reads one `r`. **§91.1 is the bug and the test that found it**: alternatives are split lazily at the column being inspected, `coverage` specialised the top-level matrix before that split ran, and an unexpanded alternative fell through as a *wildcard* — a `match` covering two of three variants was called exhaustive. **§91.3 is the part worth reading**: `Arm` gained a field, which is a compile error at every site that builds one and **silence at fourteen sites across nine files that walk one** — liveness, the frame pass, the plan's free variables, placement, the effect walk, the splitter, the graph, fusion, the evaluator. A guard those walks miss is a variable [`70`](70-the-evaluator-gets-fast-report.md) may move out from under and a slot [`70`](70-the-evaluator-gets-fast-report.md) never reserves, and none of it is a compile error. `Arm::exprs()` is [`27`](27-the-walls-come-down-report.md) §27.3's `Pattern::binders` argument one struct over, and two sites needed more than a rewrite: liveness walks an arm **backwards** (body first, guard after, because a last use is the latest read) and the frame pass **sums** a guard's slots with its body's rather than maximising, because they are sequential within one arm rather than alternatives |
| 92 | [Supply chain and release](92-supply-chain-and-release-report.md) | **Built — [`08`](08-roadmap.md) Phase 3's supply-chain bullet, plus the pipeline and installer that were on nobody's list.** `beck sbom`, `beck image` (an OCI image in one process, no apko and no daemon), `beck sign`/`verify`, `beck init ci`, a release workflow, `install.sh`, and build provenance in a public transparency log. Two things run through it: **an inventory assembled beside the thing it describes can be wrong about it**, so every gate reads a rendering rather than calling a function twice (§92.2); and a release pipeline is the one artefact that cannot be run before it is used, so the executable parts are taken out of the YAML (§92.8). §92.13 is careful about what has been executed — **no tag has been pushed** — and §92.15 names the largest gap: package signatures are not verified. |
| 93 | [The native backends](93-the-native-backends-report.md) | **Built — [`05`](05-tier-lowering.md) §5.2's dual codegen.** The whole of Lane E, in one chapter: two emitters (LLVM as text through `clang`, Cranelift through a linker), one arena of **offsets** rather than pointers, and **905 definitions compiling against 173 refused**. Its three transferable findings are not about code generation — a refusal is a claim and nothing was checking it, so four were false for whole reports at a time (§93.9); a `_` arm in a match on a representation swallowed one defect four times (§93.8); and `list_append` was refused for two reports on an argument inherited from the *evaluator's implementation strategy*, every sentence of which was true (§93.7). §93.5 is [`25`](25-benchmarks-and-expressiveness.md) §25.9's comparative claim cashed — 173× the tree-walker on `fib(30)`, within 1.2×–1.6× of Rust and C — and §93.14 is what is still not built, which is the signal vocabulary rather than the heap. |
| 94 | [The client](94-the-client-report.md) | **Built — [`05`](05-tier-lowering.md) §5.1's Mode B and the polish, except codegen and lazy routes.** The mode, the bundle, a `wasm32` kernel, data patches, reconciliation by `seq`, an offline queue that survives a cold start with the server off, freshness, routing, forms, caret and scroll, and a devtools panel — with Chromium running all of it. Three rules carry the chapter: a Mode B page may not be a function of **who is asking** (§94.2); a `Session` was one word for two facts, so eligibility and fanout stopped being the same answer (§94.3); and a **server** cannot say it is saving (§94.5). §94.12 is the measurement that matters — an interaction is 13 ms of browser CPU, 97% of it `view`, growing with the board — which is why codegen is the wrong thing to reach for first. |
| 98 | [The playground](98-playground-report.md) | **Built — [`17`](17-playground.md)'s rungs A and B.** The compiler in the browser with eleven derived answers, the whole application in the same tab with two client iframes and a `seq` scrubber, an editor that highlights and completes from the same module `beck lsp` answers from, a log that survives a reload, a self-certifying share link, and Mode B in the iframe. **The interesting half is never the WebAssembly**: twice over the work turned out to be elsewhere — dividing `beck-rt` into the half that is about the *machine* and the half that is about the *program* (§98.2), and moving the editor's answers out of `beck lsp` (§98.4). A second consumer of the compiler is what finds where its answers were reachable by one caller only. |
| 110 | [Phase 3 report, part 77](110-the-editor-edits-report.md) | **Built.** The editor edits: `beck lsp` answers **references**, **document highlight**, **prepare-rename**, **rename** and **inlay hints**, which empties [`08`](08-roadmap.md) §8.5.5's Lane C and closes the two rows [`65`](65-lsp-report.md) §65.5 called its largest gaps. The work is not the editing — it is knowing *every* place a name is written, and this compiler had four reasons why the obvious answers are wrong. A name has two accounts and an edit needs them to agree: the **lexical** one misses nothing and knows nothing, so a shadowing local and the `page` in `expect page contains` are in it; the **semantic** one knows what each reference means and is not complete, because a called reference carries the span of the *call* and editing that span replaces a call with a name. Where the two disagree, both callers decline — a rename that quietly changes a program is worse than one that does not happen — and where they agree the edits are the lexical ranges. Three things had to become true first: a test block's clauses are a grammar whose words are identifiers to the lexer, **`page` is a keyword** (so the most common name in the language had no occurrences at all and every question about it was declined), and `expect place(page)` kept no span for the name it names. The last refusal is the decisive one: the edit is **made and re-analysed**, and a rename whose result does not compile — or which still compiles as a *library* rather than as the application it was — is not offered. Over the corpus, **316 of 325 names rename**, verified compiling and publishing the same interface with one name substituted; eight decline because a signal's name is also a model field, and one declines because the edit was made and the file no longer type-checked, which is the net firing and is asserted to keep firing. The hints are the two inferred halves of a signature and nothing else — the tier §3.4 solves for and the row §3.6 infers — each labelled with the text an author could paste in at that offset, which the harness asserts by pasting it. Two findings on the way: `Def::tier_is_annotated` does not mean somebody wrote it (the linker sets it on everything it links, so every definition was hinted with the annotation it already had), and `beck fmt` is *not* wired to `textDocument/formatting` for a reason rather than by omission — the lexer skips comments, so a format-on-save would delete every `#` line in the file |
| 54 | [Ordering](54-ordering.md) | **Options, not a decision.** What [`46`](46-standard-library-report.md) §46.6 left for the next person: `Ord` as a trait, written out with its costs. The value order turns out not to be a library convenience — it is the `Map`'s iteration, the arrangements' collation, the state digest and the patch stream a replay must reproduce bit for bit — so an option that lets one type have two orders has to answer for the wire. Four options: leave it; `Ord` the way `Num` is a trait; that plus a comparator carried through every container; or give a record's *value* its declaration order. The recommendation is the last or the first, and explicitly **not** the trait — because the one thing it buys, a type overriding its own order, is the one thing that costs, and `<` is already polymorphic so a bound buys nothing. [`27`](27-the-walls-come-down-report.md) §27.8's reals are the precedent: change the representation so the single order is the right one |
| ref | [The language reference](reference/README.md) | **Generated.** The error index (102 codes), the command reference, the effect and tier matrix, the prelude and the forms — regenerated by `beck doc reference` and gated on every pull request. The design lives above; this is what a program is checked against |
| adr | [Decision records](adr/README.md) | Engineering decisions, one immutable record each — the why lives here so docs and comments can state current truth. Design decisions keep their D-numbers in [`10`](10-decisions.md) |

## The one-paragraph version

`beck` is a statically typed, homoiconic language with a Python-like surface in which the original
`(my-javascript (my-css (my-html)))` becomes literal: the page is a pure function of state, state is
a durable fold over an event stream, and hand-written JavaScript disappears — it's compiler residue.
Clients propose typed commands; one declared merge point admits time and nondeterminism; everything
downstream is deterministic, so replay, time-travel debugging and optimistic UI come from the
semantics rather than from frameworks. Placement is typed and checkable (secrets provably cannot
reach the browser), views are kept incremental by the compiler, and `beck build` emits not a binary
but a deployable system — reproducible OCI images plus a Kubernetes object graph derived from the
program's own effects — which `beck deploy` applies with typed migrations gating the rollout.
